{
  "name": "mahfazty",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling for Mahfazty — the app itself ships as plain static files with NO build step. This package.json exists only for the test suite.",
  "scripts": {
    "test:unit": "node --test \"tests/*.test.js\"",
    "test:e2e": "playwright test",
    "test": "npm run test:unit",
    "coverage": "node tests/coverage.js --min=30",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "typescript": "^6.0.3"
  }
}
