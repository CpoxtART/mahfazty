
/* ============================================================
   CONFIG
============================================================ */
const LS_PREFIX = 'walletTracker_';

const WALLET_DEFS = [
  {id:'core',        name:'Core Expenses',       initial:0, track:false, pct:'55%'},
  {id:'wishlist',    name:'Wishlist',            initial:0, track:false, pct:'10%'},
  {id:'growth',      name:'Growth',              initial:0, track:false, pct:'10%'},
  {id:'investments', name:'Investments',         initial:0, track:false, pct:'10%'},
  {id:'joy',         name:'Joy of Life',         initial:0, track:false, pct:'10%'},
  {id:'giving',      name:'Giving',              initial:0, track:false, pct:'5%'},
  // crisisOnly: hidden in normal mode, appears only when crisis/alternative mode is active
  {id:'crisis_fund', name:'Merged Reserve',      initial:0, track:false, crisisOnly:true},
  {id:'uber',        name:'Uber',                initial:0, track:true,  pct:'تتبع'},
  {id:'cards',       name:'Bank Cards',          initial:0, track:true,  pct:'تتبع'},
  {id:'cash',        name:'Cash',                initial:0, track:true,  pct:'تتبع'},
];

// Manually curated "what's new" log shown from Settings → 📋 ما الجديد؟.
// Newest entry first; add one entry per shipped feature round (not every
// commit) and keep `version` matching the CACHE bump in sw.js for that round
// — the unseen-badge logic in app.logic.js compares against CHANGELOG[0].version.
//
// Numbering: whole numbers (v47, v48, ...) are the baseline. Smaller follow-up
// rounds on top of that baseline get a decimal point instead of their own
// whole number — v47.1, v47.2, v47.3, ... up to v47.99 — then roll over to
// the next whole number (v48) and restart the decimals from there.
const CHANGELOG = [
  {
    version: 'v47.63',
    date: '2026-07-01',
    title: 'إصلاحات أمنية — CSP وحماية رمز Drive',
    items: [
      'أمان (عالي): رمز Drive OAuth لم يعد يُحفظ في localStorage الدائم — انتقل إلى sessionStorage ويختفي تلقائياً عند إغلاق المتصفح، مما يُقلّص نافذة الخطر من "حتى انتهاء الرمز" إلى "انتهاء الجلسة".',
      'أمان (عالي): كوكيز Drive أصبحت session cookies بدل كوكيز دائمة — تختفي هي الأخرى عند إغلاق المتصفح.',
      'أمان (متوسط): أُضيف مؤشر Content-Security-Policy يقيّد الموارد الخارجية المسموح بها (روابط السكريبتات، الخطوط، الاتصالات بـ API) حتى لو بقيت معالجات الأحداث المضمّنة بسبب ضرورة التوافق.',
    ],
  },
  {
    version: 'v47.62',
    date: '2026-07-01',
    title: 'تدقيق عميق متعدد الزوايا — 15 إصلاحًا',
    items: [
      'إصلاح (عالي): الفاصلة الأوروبية كفاصل عشري ("1,5" = 1.5 مش ألف وخمسة) — يعني لو كتبت "اشتراك 9,99" يحسبها صح الحين.',
      'إصلاح (عالي): حدّ أقصى 50,000 حرف في الملاحظات السريعة مع إشعار واضح عند الوصول للحدّ — لحماية الأداء من اللصق الزائد.',
      'إصلاح (عالي): شريط إشعار Google Drive يأخذ التركيز (Focus) مباشرة وبشكل صحيح لمستخدمي لوحة المفاتيح، والـ Escape يغلقه.',
      'إصلاح (عالي): زر "تراجع" في تنبيه الحذف صار له وصف واضح لقارئات الشاشة بدل مجرد ايموجي.',
      'إصلاح (عالي): حقل الملاحظات السريعة صار له تسمية نصية لقارئات الشاشة.',
      'إصلاح (متوسط): ترتيب الحفظ في التحويل بين المحافظ صار: السجل أولًا ثم الأرصدة — لو انقطع الكهرباء بينهما تبقى البيانات سليمة.',
      'إصلاح (متوسط): قائمة اختيار المحفظة (Pop-up) صارت تدعم التنقل بالأسهم (↑↓ + Home/End) بالكامل وتعلن الخيار المحدد لقارئات الشاشة.',
      'إصلاح (متوسط): في معاينة توزيع الراتب الفرق "الريال الأخير" صار يُوزَّع بنفس الطريقة الحقيقية — لا فرق بين المعاينة والنتيجة.',
      'إصلاح (متوسط): أوصاف المعاملات ذات الاتجاه المختلط (عربي + إنكليزي) تظهر بالاتجاه الصحيح تلقائيًا.',
      'إصلاح (متوسط): مبلغ الرقم في وصف توزيع الراتب معزول بشكل صحيح فلا ينعكس في السياق العربي.',
      'إصلاح (متوسط): المجموع في شاشة الاشتراكات معزول اتجاهيًا ويقرأ بالشكل الصحيح.',
      'إصلاح (متوسط): شريط الميزانية يعلن حالته ("تجاوز الميزانية / اقتراب / ضمن") لقارئات الشاشة بدل صمت تام.',
      'إصلاح (منخفض): اقتطاع اسم المحفظة بعدد حروف حقيقي — لا يعبر على وسط رمز Unicode أو emoji.',
      'إصلاح (منخفض): حقل الملاحظات السريعة يحافظ على الاتجاه الصحيح عند تغيير اللغة.',
      'إصلاح (منخفض): معاينة توزيع الراتب تطابق النتيجة الحقيقية تمامًا حتى لآخر قرش.',
    ],
  },
  {
    version: 'v47.61',
    date: '2026-06-30',
    title: 'تعرّف على المحافظ الافتراضية بأسماء عربية شائعة',
    items: [
      'إصلاح: المحافظ الجاهزة بالبرنامج مخزّنة بأسماء إنكليزية ("Cash"، "Core Expenses"...) رغم إن واجهة البرنامج عربية بالكامل — فكانت كتابة اسمها بالعربي بالملاحظات السريعة (مثل "الرئيسية" أو "بالكاش") ما تتعرّف عليها إطلاقًا. صار للمحافظ الجاهزة فقط أسماء عربية بديلة معروفة يتعرّف عليها البرنامج بالإضافة لاسمها الفعلي — المحافظ المخصّصة (اللي غيّرت اسمها بنفسك) تبقى تتعرّف عليها باسمها الحالي فقط زي ما هي.',
    ],
  },
  {
    version: 'v47.60',
    date: '2026-06-30',
    title: 'تلطيف حدّ قائمة المحفظة الرئيسية',
    items: [
      'إصلاح: التحسين السابق (v47.58) بالغ في تباين حدّ قائمة المحفظة الرئيسية — صار يبدو لامعًا وغير متناسق مع حدّ محفظة التتبّع الأزرق الأهدأ، خصوصًا بالوضع الليلي. صار للحدّ لون رمادي محايد بين الباهت والساطع، يحافظ على وضوح القائمة بدون أن "يلمع" أو يطغى على القائمة المجاورة.',
    ],
  },
  {
    version: 'v47.59',
    date: '2026-06-30',
    title: 'تعرّف أذكى على اسم المحفظة بالملاحظات السريعة',
    items: [
      'تحسين: كتابة اسم محفظة بصيغة طبيعية مع حرف جر ملتصق (مثل "بالكاش" أو "بالبنك الأهلي" أو "في الكاش") صار يتعرّف عليها البرنامج ويربط السطر بالمحفظة الصحيحة تلقائيًا، بدل ما تتطلب كتابة اسم المحفظة حرفيًا بدون أي إضافة. التعرّف يبقى مبنيًا على أسماء محافظك الفعلية فقط (مو قائمة كلمات ثابتة).',
    ],
  },
  {
    version: 'v47.58',
    date: '2026-06-30',
    title: 'دفعة تباين إضافية لقوائم اختيار المحفظة',
    items: [
      'تحسين إضافي: التحسين السابق لقائمة المحفظة الرئيسية لم يكن كافيًا — حدّها كان رماديًا خافتًا. صار الحدّ بلون النص الأساسي (بتباين كامل، بلا أي لون) وأعرض قليلًا، فيظهر كإطار واضح ومحدد بدل خط رمادي باهت.',
      'تحسين إضافي: حدّ قائمة محفظة التتبّع (المتقطّع والمصمت) صار أعرض أيضًا ليطابق وزن القائمة الرئيسية الجديد.',
    ],
  },
  {
    version: 'v47.57',
    date: '2026-06-30',
    title: 'تحسين تباين قوائم اختيار المحفظة الرئيسية والتتبّع',
    items: [
      'تحسين تصميم: قائمة اختيار المحفظة الرئيسية كانت بنفس درجة لون خلفية النافذة المحيطة تقريبًا، مما جعلها تبدو باهتة وحدودها تكاد لا تُرى — صار لها لون خلفية وحدّ أوضح (بدرجات محايدة لا تتأثر بثيمات الألوان) فتبرز بوضوح عن محيطها.',
      'تحسين تصميم: حدّ قائمة محفظة التتبّع (المتقطّع والمصمت) كان رفيعًا بعض الشيء — صار أعرض قليلًا ليعطي القائمتين معًا حضورًا بصريًا متقاربًا وتمييزًا أوضح بين الرئيسية (محايدة) والتتبّع (زرقاء).',
    ],
  },
  {
    version: 'v47.56',
    date: '2026-06-30',
    title: 'جولة تحسينات بصرية: تناسق رمز التصنيف وتوازن ألوان الثيم بالإعدادات',
    items: [
      'تناسق: تفاصيل المحفظة كانت تعرض رمز التصنيف عاديًا بلا أي لون، بينما المعاملات والتقارير يعرضانه بشارة لونية صغيرة — صار رمز التصنيف موحّد الشكل في الأماكن الثلاثة.',
      'تحسين تصميم بسيط: صف ألوان الثيم في الإعدادات (7 ألوان) كان يترك اللون السابع معلّقًا لوحده على حافة الصف الثاني — صار يتوسّط الصف بدل ذلك.',
    ],
  },
  {
    version: 'v47.55',
    date: '2026-06-30',
    title: 'شبكات أمان للتحديث والتخزين الاحتياطي + تناسق بصري في الملاحظات السريعة وسجل المعاملات',
    items: [
      'إصلاح: زر اختيار المحفظة الرئيسية في الملاحظات السريعة كان مميّزًا بلون ذهبي مختلف عن نظيره بنموذج الإضافة — تحت ثيم «الياقوتية» بالوضع النهاري كان هذا يجعله يتشابه بصريًا مع لون محفظة التتبّع الأزرق. أُزيل التمييز الذهبي وصار يطابق شكل نموذج الإضافة في كل الأوضاع والثيمات.',
      'شبكة أمان جديدة: فحص انحراف نسخة Service Worker عند إقلاع التطبيق — لو اكتشف البرنامج أن النسخة المُحمَّلة فعليًا تختلف عن النسخة المسجَّلة، يستخدم نفس مسار بانر التحديث الموجود (غير الهدّام) بدل تركه صامتًا.',
      'شبكة أمان جديدة: تحذير «تعذّر الحفظ» عند امتلاء التخزين الاحتياطي (localStorage) في المتصفحات بلا IndexedDB صار يتكرر كل بضع دقائق بدل مرة واحدة فقط بالجلسة، حتى لا يفوت المستخدم تنبيهًا مهمًا لو استمرت المشكلة.',
      'تحسينات تصميم متفرقة: توحيد أحجام خطوط كانت مكتوبة بأرقام ثابتة بدل مقياس الخطوط الموحّد للتطبيق، دمج تكرار CSS لمسافات أزرار النوافذ المنبثقة في صنف واحد، تصحيح إيموجي محفظة التتبّع في تفاصيل المحفظة والتقرير النصي، وإضافة شارة لونية صغيرة لرمز التصنيف في سجل المعاملات (بدل رمز عادي بلا لون) لتناسقه مع الشارة الملوّنة بنفس الشكل بالصفحة الرئيسية.',
    ],
  },
  {
    version: 'v47.54',
    date: '2026-06-30',
    title: 'إصلاح انعكاس ألوان محفظة التتبّع/الرئيسية + إصلاحات وصولية حرجة',
    items: [
      'إصلاح: في الملاحظات السريعة كانت محفظة التتبّع تظهر أحيانًا بلون ذهبي (لون المحفظة الرئيسية) بدل الأزرق — السبب اجتماع خللين: تمييز القائمة المنبثقة المشتركة للمحفظة المختارة كان ذهبيًا دائمًا بلا استثناء لسياق التتبّع، وتيمة الألوان «الياقوتية» كانت تستخدم درجة زرقاء تتعارض بصريًا مع لون التتبّع. تم إصلاح الاثنين والتحقق بصريًا بكل الأوضاع.',
      'إصلاح حرج: 3 قوائم اختيار محافظ أساسية (في الإضافة، التعديل، والتحويل) لم تكن تفتح أبدًا بلوحة المفاتيح (Enter/Space) — أصبحت تعمل، مع إصلاح خلل مصاحب كان يجعل الزر يفتح ثم يُغلق نفسه فورًا بسبب ازدواج التفعيل.',
      'إصلاح حرج: شارة النسبة ⚖️/ⓘ داخل بطاقة المحفظة (تفتح شاشة تفاصيل المحفظة) كانت غير قابلة للوصول بلوحة المفاتيح إطلاقًا — تم إصلاحها.',
      'إصلاحات وصولية: تحسين تباين النص الأزرق الخافت في الوضع الداكن، إضافة مؤشر تركيز واضح لقوائم المحافظ المنبثقة، إعادة التركيز لمكانه الصحيح بعد إغلاق القائمة بدل فقدانه بالصفحة، وربط رسالة تحذير «المبلغ ناقص» بحقل الإدخال لقارئات الشاشة.',
      'إصلاح: ألوان أزرار الدخل/المصروف والتحويل وبعض الحدود كانت ثابتة على درجات الوضع الداكن حتى في الوضع الفاتح — صححت لتطابق ثيم كل وضع.',
      'تحسينات تصميم بسيطة: حجم أيقونة مزامنة Google Drive في الترويسة كان أصغر من باقي الأيقونات، ووزن خط محفظة التتبّع بالملاحظات السريعة كان غامقًا دائمًا بعكس نظيرتها بنموذج الإضافة.',
    ],
  },
  {
    version: 'v47.53',
    date: '2026-06-30',
    title: 'إصلاح حرج: إغلاق نموذج التحويل بين المحافظ كان يخرج من التطبيق',
    items: [
      'إصلاح حرج: الضغط على «إلغاء»/إغلاق بعد فتح «تحويل بين المحافظ» من نموذج الإضافة (أو بدء أول دخل من شاشة الترحيب) كان أحيانًا يُخرج من التطبيق فعليًا بدل إغلاق النافذة فقط — كان السبب تعارضًا داخليًا في إدارة سجل التصفح، وتم إصلاحه.',
      'تمييز أوضح لمحفظة التتبّع: قائمتها (في نموذج الإضافة وفي كل سطر بالملاحظات السريعة) صارت بلون أزرق دائمًا — حتى قبل اختيار محفظة — لتتميّز بوضوح عن المحفظة الرئيسية الذهبية فوقها.',
      'ميزة جديدة بالملاحظات السريعة: اكتب اسم محفظتك (الرئيسية أو التتبّع) كآخر كلمة/كلمات بالسطر — مثل «قهوة 15 أوبر» — وسيتعرّف عليها البرنامج تلقائيًا ويحدد المحفظة المناسبة لتلك المعاملة بدل تركها على المحفظة الافتراضية.',
    ],
  },
  {
    version: 'v47.52',
    date: '2026-06-29',
    title: 'كل قوائم المحافظ صارت قائمة داخلية أنيقة (لا قائمة نظام)',
    items: [
      'تغيير: محفظة التتبّع في نموذج الإضافة + قائمتا كل سطر بالملاحظات (الرئيسية والتتبّع) صارت كلها تفتح <b>قائمة منبثقة داخل الصفحة</b> بنفس شكل منتقي المحفظة الرئيسي — لا قوائم نظام، ومقروءة في كل الثيمات.',
      'محرّك واحد مشترك للقوائم: داكنة بالداكن، تعرض اسم المحفظة ورصيدها، وتُغلق بالنقر خارجها أو Escape.',
    ],
  },
  {
    version: 'v47.51',
    date: '2026-06-29',
    title: 'إصلاح: قوائم المحافظ المنسدلة تطلع فاتحة/غير مقروءة بالوضع الداكن',
    items: [
      'إصلاح: قوائم اختيار المحفظة (التتبّع بالإضافة + قائمتا كل سطر بالملاحظات) كانت تفتح بخلفية فاتحة/نص غير واضح في الوضع الداكن — أُضيف color-scheme فصارت تتبع ألوان الثيم.',
      'تذكير: لو ما ظهرت التغييرات (مثل اختفاء رموز @ من الملاحظات) فالنسخة المخزّنة قديمة — حدّثها من ⚙ الإعدادات ← زر «مسح الكاش/تحديث»، أو أغلق التطبيق تمامًا وافتحه.',
    ],
  },
  {
    version: 'v47.50',
    date: '2026-06-29',
    title: 'محفظة التتبّع صارت قائمة منسدلة دائمة (لا زر تفعيل) + حذف رموز @ من الملاحظات',
    items: [
      'تغيير: في نموذج الإضافة، محفظة التتبّع صارت <b>قائمة منسدلة دائمة الظهور</b> (بتصميم أزرق مميّز) بدل زر التفعيل/الصح — تختارها مباشرةً مثل المحفظة الرئيسية، و«بدون تتبّع» هو الخيار الأول.',
      'حذف: أُزيلت رموز @ نهائيًا من الملاحظات السريعة — لا حاجة لكتابة أي رمز.',
      'تناسق: بعد «حوّل لمعاملات»، كل سطر يطلع له قائمتان منسدلتان مثل نموذج الإضافة: <b>👛 المحفظة الرئيسية</b> و<b>🏦 محفظة التتبّع</b> — تختار منهما المحفظتين مباشرةً (وحدة الصرف ووحدة التتبّع).',
    ],
  },
  {
    version: 'v47.49',
    date: '2026-06-29',
    title: 'نموذج محافظ موحّد: رئيسي + تتبّع في كل مكان (تصحيح تناسق الملاحظات)',
    items: [
      'تصحيح: أُرجعت محافظ التتبّع من منتقي المحفظة الرئيسية — صارت تُعيَّن من قسمها الخاص (تتبّع) كما في نموذج الإضافة، فلا خلط.',
      'تناسق: في معاينة الملاحظات السريعة صار لكل سطر قائمتان ثابتتان مثل نموذج الإضافة: <b>👛 المحفظة الرئيسية</b> (ذهبية) و<b>🏦 محفظة تتبّع</b> اختيارية (زرقاء).',
      'تحسين: الصيغة @المحفظة صارت ذكية — اسم محفظة ميزانية يضبط الرئيسية، واسم محفظة تتبّع يضبط التتبّع تلقائيًا، ويمكن الاثنان في سطر واحد (مثال: «@رغبات @كاش قهوة ١٥»). تعمل كعناوين تنطبق على ما تحتها أو داخل السطر.',
      'إصلاح: تلميح المعاينة كان يُظهر وسوم <b> كنص؛ صار يُعرض منسّقًا.',
    ],
  },
  {
    version: 'v47.48',
    date: '2026-06-29',
    title: 'محافظ التتبع صارت أساسية + فرز محافظ الملاحظات قبل التحويل + إصلاح قصّ الأسماء بالوضع البديل',
    items: [
      'إصلاح: أسماء المحافظ (خاصة في الوضع البديل مثل «الاحتياطي المدمج» و«Core Expenses») لم تعد تُقصّ — تلتفّ على سطرين مع محاذاة متناسقة للبطاقات.',
      'جديد: محافظ التتبع (أوبر/كاش/بطاقات) صارت محافظ أساسية قابلة للاختيار في نموذج الإضافة وفي بانر الملاحظات — للمصروف وللدخل. (تبقى غير محتسبة بالإجمالي المتاح للصرف، ولا تدخل في التوزيع التلقائي.)',
      'سلوك: الدخل المُسجَّل في محفظة تتبع لا يُوزَّع تلقائيًا — يبقى في محفظته كعدّاد مستقل.',
      'جديد وأهم: في الملاحظات السريعة تقدر تفرز المحافظ <b>قبل التحويل</b> — اكتب سطر محفظة مثل «@كاش» أو «@اوبر» فينطبق على كل الأسطر تحته حتى السطر التالي، أو ضع «@المحفظة» داخل أي سطر. حلٌّ لمن لديه ٢٠+ محفظة بدل تعديل كل معاملة يدويًا.',
      'تحسين: في معاينة الملاحظات صارت محفظة كل سطر قائمة ذهبية بارزة مع 👛، والتلميحات تشرح الصيغة @المحفظة وتشمل محافظ التتبع.',
    ],
  },
  {
    version: 'v47.47',
    date: '2026-06-29',
    title: 'إصلاح قصّ أسماء المحافظ + توضيح ميزة محفظة كل سطر في الملاحظات',
    items: [
      'إصلاح: اسم المحفظة في البطاقة كان يُقصّ بشكل نشاز (مثل «...e Expenses» بنقاط على اليسار). الآن يستخدم كامل المساحة المتاحة، والقصّ (عند الضرورة القصوى) يقع في نهايته الطبيعية، وتتّسع كل الأسماء الافتراضية على الشاشات الشائعة.',
      'تحسين: في معاينة الملاحظات السريعة صارت محفظة كل سطر بقائمة ذهبية بارزة مع أيقونة 👛 — توضّح أن لكل معاملة محفظتها وتقدر توزّع المعاملات على محافظ مختلفة (كانت الميزة غير ظاهرة).',
      'تحسين: نصوص إرشادية أوضح — شرائح المحفظة الافتراضية تُبيّن أنها لكل الأسطر، والمعاينة تشرح كيف تعطي كل معاملة محفظتها.',
    ],
  },
  {
    version: 'v47.46',
    date: '2026-06-29',
    title: 'تحليل عميق (الجولة السادسة): تزامن وأرقام وسلامة الملاحظات السريعة',
    items: [
      'إصلاح عالٍ: الملاحظات السريعة تتحقق من المحافظ مقابل أحدث تعريف محفوظ عند الحفظ — معاملة لمحفظة حُذفت في تبويب آخر (أثناء فتح النافذة) كانت تُسجَّل ثم تُفقد بصمت عند إعادة التحميل؛ الآن تُحوَّل للمحفظة الافتراضية مع تنبيه.',
      'إصلاح متوسط: حارس كتابة متقاطع — منع تداخل عمليتي كتابة (إضافة/تعديل/تحويل/حذف/ملاحظات) عبر نقاط await، كان يمكن أن يفسد الأرصدة في توقيت نادر.',
      'إصلاح متوسط: اختصار الأرقام في الرسوم (مثل 999,950) كان يعرض «1000K» بدل «1M» — صُحِّحت العتبات وأُضيفت درجة المليار.',
      'إصلاح متوسط: حدّ أقصى 300 سطر لدفعة الملاحظات السريعة (منع تجميد) + طوابع زمنية متمايزة وغير مستقبلية تحفظ ترتيب الدفعة.',
      'إصلاح متوسط: مُعرّفات معاملات الملاحظات السريعة صارت 5 أحرف عشوائية (كبقية التطبيق) لتقليل احتمال التصادم.',
      'إصلاح منخفض: checkBalanceDrift يستخدم round2 (كمسار الإصلاح) فلا ينبثق تنبيه انحراف وهمي لا يُصلِح شيئًا.',
      'تحسين: إلغاء إطار الرسم المعلّق عند التبديل السريع للتبويبات؛ عزل اتجاهي (bidi) لمبلغ تنبيه الرصيد؛ توحيد اتجاه شارة المقارنة في الرسم؛ تعزيز مسار التحديث القسري.',
    ],
  },
  {
    version: 'v47.45',
    date: '2026-06-29',
    title: 'توحيد دلالي لأحجام القيم النقدية (إيقاع بصري أوضح)',
    items: [
      'تحسين: القيمة النقدية صارت تتبع المعنى لا المكان — أربع درجات موحّدة (--money-*) بدل ٦ أحجام مرتجلة (38/19/17/16/15/14).',
      'تغيير بصري: كل القيم الإحصائية الثانوية توحّدت على 16px — أرقام تفاصيل المحفظة (17←16) وأرقام ملخّص الدخل/المصروف/الصافي (15←16) — فصار للعين تدرّج واحد واضح: رصيد رئيسي ← قيمة محفظة ← قيمة إحصائية ← مبلغ معاملة.',
    ],
  },
  {
    version: 'v47.44',
    date: '2026-06-29',
    title: 'توحيد نظام التصميم: سلّم خطوط بمتغيرات + اتساق الحدود والحركات والتباعد',
    items: [
      'جديد: سلّم خطوط موحّد بمتغيرات (--fs-*) يغذّي كل أحجام الخط من مصدر واحد بدل القيم المتناثرة (152 موضعًا) — بلا أي تغيير بصري، مع طيّ القيم الكسرية (11.5/12.5px) على أقرب درجة.',
      'اتساق: توحيد سُمك حدود الأزرار على 1px عبر متغيّر --bd — كانت الأزرار الثانوية/الخطرة 1.5px بينما البقية 1px.',
      'اتساق: استبدال قيمة الانتقال الثابتة .15s بمتغيّر --dur-fast في 51 موضعًا لتتبع سلّم الحركة الموحّد.',
      'اتساق: توحيد حشو أزرار الصفوف على 13px (مطابقًا لأزرار النوافذ)، وتنظيف هوامش بطاقة المحفظة الشاذّة (9px ← 8px).',
    ],
  },
  {
    version: 'v47.43',
    date: '2026-06-29',
    title: 'تحسين تجربة وتصميم ميزة «الملاحظات السريعة» (تحليل بصري عميق)',
    items: [
      'إصلاح حرج: حقول الملاحظات السريعة (مربع النص وحقول الوصف/المبلغ) كانت تظهر بخلفية بيضاء ونص أسود في الوضع الداكن/الأسود — الآن تتبع ألوان الثيم بالكامل مع حلقة تركيز موحّدة.',
      'إصلاح عالٍ: شارة «مسودة» على البانر صارت تستخدم زوج ألوان الزر المتباين — كانت غير مقروءة في الوضع الفاتح.',
      'إصلاح عالٍ: أهداف اللمس لأزرار النوع والحذف في المعاينة وُسّعت إلى 44px (منطقة لمس خفية) لتفادي النقر الخاطئ.',
      'إصلاح عالٍ: زر «سجّل الكل» يتعطّل بصريًا عند عدم وجود سطر صالح، والعدّاد يعرض «الصالح/الإجمالي» دائمًا.',
      'إصلاح: حذف كل أسطر المعاينة يطوي المعاينة ويعيد التركيز لمربع النص بدل ترك منطقة فارغة محيّرة.',
      'إصلاح: كل سطر غير صالح يعرض سبب صريح «أضِف سعرًا» بدل الاعتماد على اللون فقط (وصولية).',
      'تحسين وصولية: نقل التركيز للمعاينة بعد التحويل، تسميات ARIA ديناميكية لزر النوع، شرائح المحفظة كمجموعة اختيار (radiogroup)، وتسمية أيقونة الفئة.',
      'إصلاح تجاوب: أسماء المحافظ الطويلة لم تعد تتجاوز عرض الشاشة (قصّ ذكي + منتقي مرن)، وأُضيف تكيّف للشاشات الضيقة (≤360px).',
      'إصلاح RTL: عدّاد المعاينة «٣ / ٤» يُعرض باتجاه صحيح.',
      'اتساق: توهّج زر الإضافة العائم وأثر زر الأدوات يتبعان لون التطبيق المختار بدل الذهبي الثابت؛ وأزرار بانر التحديث رفعت لـ44px؛ وتوحيد زوايا عناصر الميزة على متغيرات النظام.',
    ],
  },
  {
    version: 'v47.42',
    date: '2026-06-29',
    title: 'إصلاح حاسم: بانر الملاحظات السريعة يرجع تحت «الوضع البديل»',
    items: [
      'إصلاح: عند من كان ترتيبه المحفوظ (محليًا أو عبر Drive) يضع قسم الملاحظات السريعة في النهاية، صار يُعاد لمكانه تحت «الوضع البديل» تلقائيًا في كل فتح — إصلاح v47.41 السابق كان يعالج فقط حالة غياب القسم لا حالة كونه «عالقًا» في النهاية.',
      'سلوك: إذا رتّبت أقسام الصفحة الرئيسية يدويًا من محرّر التخطيط، يُحترم ترتيبك ويتوقف إعادة التموضع التلقائي.',
    ],
  },
  {
    version: 'v47.41',
    date: '2026-06-29',
    title: 'إصلاح: بانر «الملاحظات السريعة» ما يظهر للمستخدمين الحاليين',
    items: [
      'إصلاح: القسم الجديد (بانر الملاحظات السريعة) كان يُضاف في آخر الصفحة الرئيسية للمستخدمين الذين لديهم ترتيب أقسام محفوظ مسبقًا — فيظهر أسفل كل المحافظ بدل أن يكون تحت «الوضع البديل».',
      'تحسين: sanitizeOrder تُدرج أي قسم جديد في موضعه الافتراضي (بعد جاره المناسب) بدل إلحاقه في النهاية دائمًا — يضمن ظهور الأقسام المستقبلية في مكانها الصحيح لكل المستخدمين.',
    ],
  },
  {
    version: 'v47.40',
    date: '2026-06-29',
    title: 'تحسين: محفظة مستقلة لكل سطر في «ملاحظات سريعة ← معاملات»',
    items: [
      'جديد: في معاينة الملاحظات السريعة، صار لكل سطر منتقي محفظة خاص — تقدر توجّه بعض المعاملات لمحفظة وبعضها لمحفظة ثانية في نفس الدفعة (مثلاً ٣ معاملات لمحفظة و٣ لأخرى).',
      'تغيير: شرائح المحفظة بالأعلى صارت «المحفظة الافتراضية» — تُطبَّق على كل سطر عند التحويل، وتقدر تغيّرها لكل سطر بعدها.',
      'تحسين: صفوف المعاينة أصبحت بسطرين (نوع/وصف/حذف ثم فئة/محفظة/مبلغ) لاستيعاب منتقي المحفظة بوضوح على شاشات الجوال.',
    ],
  },
  {
    version: 'v47.39',
    date: '2026-06-29',
    title: 'جديد: ملاحظات سريعة ← معاملات (تحويل تلقائي من نص حر)',
    items: [
      'جديد: بانر «ملاحظات سريعة ← معاملات» في الصفحة الرئيسية (تحت الوضع البديل) — اكتب ملاحظاتك بحرية، سطر لكل معاملة (الوصف وسعره)، والبرنامج يحوّلها معاملات جاهزة.',
      'جديد: محرّك تحليل ذكي يقتبس المبلغ والوصف من كل سطر، ويخمّن الفئة تلقائيًا (طعام/مواصلات/فواتير…)، ويكتشف الدخل عبر علامة + أو كلمات مثل «راتب/دخل».',
      'جديد: شاشة معاينة قبل الحفظ — عدّل الوصف أو المبلغ، بدّل النوع (دخل/مصروف)، أو احذف أي سطر، ثم سجّل الكل دفعةً واحدة.',
      'جديد: مسوّدة الملاحظات تُحفظ تلقائيًا وتبقى عبر إعادة التشغيل والأجهزة — دوّن طوال اليوم وحوّلها وقت ما يناسبك. شارة على البانر تعرض عدد الأسطر المعلّقة.',
      'جديد: شريحة اختيار المحفظة المستهدفة، ودعم كامل للأرقام العربية، وتوزيع الدخل تلقائيًا إذا كان مفعّلًا.',
      'جديد: شريحة شرح للميزة في جولة الترحيب بأول فتح للبرنامج.',
    ],
  },
  {
    version: 'v47.38',
    date: '2026-06-29',
    title: 'تحليل عميق (الجولة الرابعة): إصلاحات مزامنة واستيراد وأداء من زوايا جديدة',
    items: [
      'إصلاح عالٍ: إغلاق نافذة تعارض Drive عبر زر الرجوع/Escape بدون اختيار كان يُجمِّد مؤشر المزامنة على "يزامن" إلى الأبد ويترك نسخة السحابة معلّقة — الآن يُلغى التعارض بأمان ويعود المؤشر لحالة "جاهز" مع إبقاء البيانات المحلية.',
      'إصلاح عالٍ: مزامنة التبويبات — التبويب المستقبِل كان قد يقرأ نسخة IndexedDB قديمة قبل اكتمال كتابة التبويب الآخر (نافذة الـ 400ms)؛ زِيد تأخير القراءة الأولي إلى 600ms ليغطيها.',
      'إصلاح متوسط: مطابقة الاشتراكات المتتبَّعة صارت باتجاه واحد فقط (الوصف يحتوي اسم الاشتراك) مع حد أدنى 3 أحرف — يمنع اسم اشتراك قصير من كبت معاملات غير مرتبطة بالخطأ.',
      'إصلاح متوسط: applyImport يحد عدد المعاملات المستوردة بـ 100,000 مع تنبيه — يمنع تجميد المتصفح عند ملف ضخم/تالف.',
      'إصلاح متوسط: exportData لا يضع JSON ضخماً في حقل المعاينة (يبقى التنزيل كاملاً) — يمنع تجميد الواجهة عند بيانات كبيرة.',
      'إصلاح متوسط: تحسين أداء قائمة المعاملات — تُبنى خارج DOM في DocumentFragment وتُلحَق دفعةً واحدة بدلاً من reflow لكل صف (حتى 500 صف).',
      'إصلاح منخفض: buildTxTs وsaveEdit يضعان حداً أدنى للتاريخ عند 2010 — تاريخ خارج النطاق برمجياً لم يعد يُنشئ طابعاً زمنياً يُثبِّت المعاملة أعلى كل القوائم.',
      'إصلاح منخفض: detectRecurring يُبطل الكاش عند تغيير اسم/مبلغ اشتراك (وليس عدده فقط).',
      'إصلاح منخفض: فلتر الاشتراكات المتتبَّعة يستبعد ذوات المبلغ صفر مبكراً.',
      'إصلاح منخفض: مقارنة وقت نسخة Drive محصّنة ضد طابع ISO تالف (Date.parse → NaN) لا يُفسد المقارنة.',
      'إصلاح منخفض: applyImport يطبّق الـ tombstones فوراً على المجموعة المستوردة (لا ينتظر إعادة التحميل).',
    ],
  },
  {
    version: 'v47.37',
    date: '2026-06-26',
    title: 'تحليل عميق (الجولة الثالثة): 19 إصلاحاً من زوايا تحليلية جديدة',
    items: [
      'إصلاح عالٍ: repeatLastTx لا تختار محفظة crisisOnly (كـ crisis_fund) إذا كانت مخفية في الوضع العادي — كانت تكتب المعاملة إلى محفظة غير مرئية بصمت.',
      'إصلاح عالٍ: undoDelete تحفظ saveConfig (إزالة الـ tombstone) قبل saveTx — يمنع crash بين الحفظين من إعادة إخفاء المعاملة المُستعادة عند إعادة التحميل.',
      'إصلاح متوسط: saveEdit لا تُبطل _txMutationStamp عند حالات الإرجاع المبكر (معاملة غير موجودة أو مبلغ غير صحيح) — يمنع إبطال كاش غير ضروري.',
      'إصلاح متوسط: closeModal وcloseAddDrawer لا تسحبان من _focusStack إذا كان الـ overlay مغلقاً أصلاً — يمنع تلف تسلسل التركيز عند استدعائهما على overlay مغلق.',
      'إصلاح متوسط: انقطاع الإنترنت يُظهر الآن toast للمستخدم بدلاً من تحديث مؤشر Drive الصامت فقط.',
      'إصلاح متوسط: renderRecentTx تحترم الآن walletFilter وcategoryFilter وsearchQuery — تبويب المعاملات يعكس الفلتر النشط بدلاً من عرض كل المعاملات دائماً.',
      'إصلاح متوسط: setWalletFilter وclearWalletFilter يُحدِّثان قائمة تبويب المعاملات فور تغيير الفلتر.',
      'إصلاح متوسط: onSearchInput وclearSearch يُحدِّثان تبويب المعاملات عند البحث من تلك الشاشة.',
      'إصلاح متوسط: saveWalletBudget تتحقق من وجود المحفظة — محفظة محذوفة من تبويب آخر أثناء فتح نافذة التفاصيل تُغلق بتنبيه بدلاً من استثناء صامت.',
      'إصلاح متوسط: قائمة تفاصيل المحفظة تُظهر الآن تلميح "X معاملة أقدم" عند تجاوز 50 معاملة.',
      'إصلاح متوسط-منخفض: computeRenderSig تشمل currentTab — تحديثات cross-tab لن تتخطى إعادة الرسم بعد تغيير التبويب مع بيانات ثابتة.',
      'إصلاح متوسط-منخفض: loadState تستخدم round2() بدلاً من Math.round()*100/100 في ثلاثة مواضع — يزيل انجراف IEEE-754 عند استعادة الأرصدة.',
      'إصلاح متوسط-منخفض: saveConfig ترجع true/false — المُستدعيات يمكنها الآن الكشف عن فشل الحفظ.',
      'إصلاح متوسط-منخفض: _pruneRecurringDismissals تحد الحجم بـ 200 مدخل — يمنع تجاوز حصة localStorage عندما تطابق جميع الأنماط معاملات حية.',
      'إصلاح منخفض: _wireGrabber — snap-back يستخدم rAF لضمان تفعيل CSS transition قبل إعادة transform، يمنع الحركة المتقطعة عند touchcancel.',
      'إصلاح منخفض: _nextPushOverlayReplaces = false في try/finally — يُضمن إعادة الضبط حتى لو openDistributionModal رمى استثناءً.',
      'إصلاح منخفض: sanitizeBudgets تحد القيم بـ MAX_AMOUNT وتطبق round2 — تمنع قيم ضخمة من إظهار شريط ميزانية صفري دائماً.',
      'إصلاح منخفض: نص المجموع في مركز مخطط الدائرة مُقيَّد بحدود الثقب الداخلي — أرقام كبيرة لا تتجاوز الحلقة بعد الآن.',
      'إصلاح منخفض: _heroStatsSig تشمل _txMutationStamp — كاش إحصائيات البطل يبطل دائماً عند أي تعديل (حماية إضافية لأي مسار يستدعيها مباشرةً).',
    ],
  },
  {
    version: 'v47.36',
    date: '2026-06-26',
    title: 'تحليل عميق (الجولة الثانية): 20 إصلاحاً من زوايا تحليلية جديدة',
    items: [
      'إصلاح عالٍ: _editingDistSource تنشّط عند وجود ضلع واحد مرتبط (>= 1) بدلاً من > 1 — كانت مصادر التوزيع ذات الضلع اليتيم تُفلت من القفل.',
      'إصلاح عالٍ: حذف ضلع توزيع دخل (_distributionLeg) يعطي تنبيهاً بدلاً من حذف المجموعة كاملاً بصمت.',
      'إصلاح عالٍ: محرر التوزيع يعمل الآن على نسخة مسودة — إغلاق الإعدادات بدون حفظ لا يُطبق التغييرات على النسب الحية.',
      'إصلاح عالٍ: زر اختيار محفظة معاملة التحويل يُقفل بـ tabIndex=-1 وaria-disabled أيضاً (وليس CSS فقط) لمنع الوصول لوحة المفاتيح.',
      'إصلاح عالٍ: saveEdit يطبّق قفل المحفظة برمجياً لمعاملات التحويل بالإضافة للقيد المرئي.',
      'إصلاح عالٍ: saveEdit تسمح بتعديل الوصف/التاريخ/النوع لمصادر الدخل الموزعة — كانت تعيد الخطأ لأي حفظ حتى لو المبلغ لم يتغير.',
      'إصلاح متوسط: runDistribution تُعيد قيمة منطقية — addTx لا تُظهر "تم التوزيع تلقائياً" إذا لم يكن هناك ما يُوزَّع فعلاً.',
      'إصلاح متوسط: pruneTombstones تُستدعى بعد عمليات الحذف الشاملة (wipeAll/clearBalancesAndTx) لمنع تجاوز حصة localStorage.',
      'إصلاح متوسط: closeModal للـ editModal يُصفِّر _editingDistSource و_editingTransferLeg ويعيد tabIndex زر المحفظة.',
      'إصلاح متوسط: deleteFromEdit يستخدم try/finally لضمان إغلاق النافذة حتى لو deleteTx رمى استثناءً.',
      'إصلاح متوسط: saveWalletBudget تتحقق من صحة المدخل — حقل غير رقمي يُظهر تنبيهاً بدلاً من حذف الميزانية بصمت.',
      'إصلاح متوسط: doTransfer يحتسب الرصيد المدمج لـ crisis_fund عند التحقق من الرصيد الكافي في الوضع البديل.',
      'إصلاح متوسط: sumExpenses وrenderAnalytics تحترمان walletFilter — بطاقات التحليلات كانت تعرض مجموع كل المحافظ بغض النظر عن الفلتر.',
      'إصلاح متوسط: applyImport تنظّف مراجع trackWallet اليتيمة التي تشير لمحافظ غير موجودة في النسخة المستوردة.',
      'إصلاح: switchTab للمعاملات يُصفِّر _recentVisibleCount — التبديل بين التبويبات لم يكن يُعيد الصفحة الأولى.',
      'إصلاح: saveSubModal تقارن الأسماء المكررة بدون حساسية الحالة (case-insensitive).',
      'إصلاح: moveWalletDef تعيد ترتيب DISTRIBUTION ليطابق ترتيب المحافظ الجديد.',
      'إصلاح: renderChart يستخدم Math.max(1, points.length-1) لمنع القسمة على صفر (حارس دفاعي).',
    ],
  },
  {
    version: 'v47.35',
    date: '2026-06-25',
    title: 'تحليل عميق: 24 إصلاحاً لأخطاء المنطق والأداء وتجربة المستخدم',
    items: [
      'إصلاح حرج: driveSyncToCloud كانت تُرسل البيانات بدون walletDefs — المحافظ المخصصة كانت تُفقد على الأجهزة الأخرى عبر Drive.',
      'إصلاح حرج: sanitizeWalletDefs تتحقق الآن من صيغة id المحفظة (أحرف آمنة فقط) للحماية من XSS في ملفات النسخ الاحتياطية المُعدَّلة.',
      'إصلاح عالٍ: applyWalletDefs تُصحح الآن crisis_fund التي تفتقر لـ crisisOnly في النسخ القديمة بدلاً من تجاهلها.',
      'إصلاح عالٍ: saveBalances لا تُحدِّث dataEdit إذا فشل الحفظ بسبب امتلاء التخزين — يمنع IDB الأحدث من أن يُهزم بنسخة فارغة.',
      'إصلاح عالٍ: applyImport تختم الآن dataEdit بالوقت الحالي بدلاً من وقت النسخة الاحتياطية — يمنع Drive من إعادة دمج البيانات السحابية فوق الاستيراد الجديد.',
      'إصلاح عالٍ: adoptCloudSnapshot تتحقق الآن من حقل id في كل معاملة وتزيل المكررات (كما في applyImport).',
      'إصلاح عالٍ: toggleCrisis تُصفِّي walletFilter عند الخروج من الوضع البديل إذا كانت تشير لمحفظة crisisOnly مخفية.',
      'إصلاح عالٍ: controllerchange لا يُعيد تحميل سوى النافذة التي أطلقت التحديث — باقي النوافذ لا تُعاد تلقائياً.',
      'إصلاح: reconcileBalances تستخدم round2 بدلاً من Math.round لمنع تراكم أخطاء التقريب.',
      'إصلاح: loadState تدمج tombstones من IDB مع localStorage (union merge) بدلاً من تجاهل IDB عند وجود أي tombstone محلي.',
      'إصلاح: parseAmount ترفض الآن المدخلات ذات النقطتين مثل 1.2.3 بدلاً من تمريرها كـ 1.2.',
      'إصلاح: showUpdateBanner لا تُنشئ timer مزدوجاً إذا استُدعيت مرتين قبل ظهور الـ CSS class.',
      'إصلاح: dismissUpdate تُصفِّي _pendingWorker لمنع الإشارة لـ SW قديم بعد رفض التحديث.',
      'إصلاح: matchesTrackedSub تتجاهل الاشتراكات التي يصبح اسمها فارغاً بعد التطبيع.',
      'إصلاح: barMax في شبكة المحافظ يحتسب القيمة المدمجة لـ crisis_fund في الوضع البديل.',
      'إصلاح: openWalletDetail يطبق round2 على إجمالي الدخل والمصروف.',
      'إصلاح: exportMonthlyReport وبuildDailyReviewContent يستخدمان round2 في التجميع.',
      'إصلاح: _initQuickAmountSync يستخدم parseAmount للمقارنة بدلاً من parseFloat يدوياً.',
      'إصلاح: normalizeDigits تحوِّل الإشارة السالبة Unicode (−) إلى ASCII hyphen.',
      'إصلاح: نسبة core في WALLET_DEFS صُحِّحت من 50% إلى 55%.',
      'إصلاح: remainderPct في نافذة التوزيع محسوبة من نفس الأساس لمنع عرض 0%.',
      'تحسين: زر فلاتر التقارير min-height أصبح 44px (معيار touch target).',
    ],
  },
  {
    version: 'v47.34',
    date: '2026-06-25',
    title: 'إصلاح: رصيد محفظة Merged Reserve يظهر صفراً في الوضع البديل',
    items: [
      'إصلاح: بطاقة "الاحتياطي المدمج" في شبكة المحافظ ومنتقي الإضافة كانت تعرض رصيد crisis_fund فقط (0.00) بدلاً من مجموع المحافظ المخفية (wishlist + growth + investments + joy + giving) + رصيد crisis_fund.',
      'تغيير: حُذفت بطاقة "الاحتياطي البديل (مدمج)" الإضافية — بطاقة Merged Reserve الموجودة أصبحت تعرض الرصيد الكامل مباشرةً.',
    ],
  },
  {
    version: 'v47.33',
    date: '2026-06-25',
    title: 'بانر التحديث يظهر ويُحدِّث تلقائياً + إشعار بعد التحديث في الإعدادات',
    items: [
      'تغيير: بانر "تحديث جديد متاح" يظهر الآن عند اكتشاف نسخة جديدة، ويُطبِّق التحديث تلقائياً بعد 8 ثوانٍ أو فور الضغط على "تحديث الآن".',
      'جديد: بعد إعادة التحميل التلقائية، يظهر إشعار يؤكد النسخة الجديدة مع دعوة لفتح الإعدادات لمعرفة الجديد.',
      'إصلاح: Service Worker لم يعد يستدعي skipWaiting تلقائياً — التحديث الآن يمر عبر البانر بدلاً من إعادة التحميل الصامتة.',
    ],
  },
  {
    version: 'v47.32',
    date: '2026-06-25',
    title: 'إصلاح: Merged Reserve لا تظهر في قائمة المحافظ عند الإضافة في الوضع البديل',
    items: [
      'إصلاح: sanitizeWalletDefs كانت تحذف خاصية crisisOnly عند إعادة تحميل المحافظ المحفوظة — النتيجة: crisis_fund تختفي من قائمة الإضافة في الوضع البديل.',
      'إصلاح: applyWalletDefs تضمن الآن وجود crisis_fund دائماً حتى لو ملف الحفظ القديم لا يحتويها.',
      'تغيير: اسم المحفظة أصبح "Merged Reserve" بالإنجليزية.',
    ],
  },
  {
    version: 'v47.31',
    date: '2026-06-25',
    title: 'الوضع البديل: محفظة "الاحتياطي المدمج" تظهر فقط عند التفعيل',
    items: [
      'جديد: محفظة "الاحتياطي المدمج" (crisis_fund) مخفية في الوضع العادي وتظهر تلقائيًا فقط عند تفعيل الوضع البديل — يمكن تسجيل المعاملات إليها مباشرةً.',
      'تغيير: حذف محفظة Reserve — نسبة التوزيع الخاصة بها (5%) أُضيفت إلى Core Expenses (أصبحت 55%).',
      'تغيير: الوضع البديل يعرض الآن: Core Expenses + الاحتياطي المدمج + محافظ التتبع فقط.',
    ],
  },
  {
    version: 'v47.30',
    date: '2026-06-25',
    title: 'الوضع البديل: يُظهر الآن محفظتي الأساسيات والاحتياطي فقط',
    items: [
      'تغيير: الوضع البديل يُظهر الآن محفظة Core Expenses + محفظة Reserve فقط (بالإضافة لمحافظ التتبع) — بدلاً من إظهار الأساسيات فقط.',
    ],
  },
  {
    version: 'v47.29',
    date: '2026-06-25',
    title: 'إصلاح حرج: قائمة المحافظ في نموذج الإضافة لا تتغير عند تفعيل الوضع البديل',
    items: [
      'إصلاح (حرج): عند تفعيل الوضع البديل، كانت قائمة اختيار المحفظة في نموذج تسجيل المصروف/الدخل تعرض كل المحافظ بدلاً من المحافظ المتاحة في الوضع البديل فقط — الإصلاح: recomputeSelectableWallets تراعي الآن حالة الوضع البديل وتُستدعى عند تفعيله/إيقافه وعند تحميل التطبيق.',
    ],
  },
  {
    version: 'v47.28',
    date: '2026-06-24',
    title: 'إصلاح شامل: أمان التوزيع، دقة الأرقام، استقرار التطبيق وتحسينات UX',
    items: [
      'إصلاح (حرج): توزيع الدخل كان يُودع في المحفظة المصدر مرة ثانية إذا كانت ضمن قائمة النسب — تم استثناؤها تلقائيًا.',
      'إصلاح (حرج): كان رصيد المحفظة بعد استيراد النسخة الاحتياطية قد يتضارب مع المعاملات إذا أزال الاستيراد أرجلًا معزولة — تم إضافة إعادة حساب الرصيد (reconcileBalances) فور الاستيراد.',
      'إصلاح (عالٍ): Back-Forward Cache — عند استعادة الصفحة من ذاكرة التصفح (bfcache)، قد تتعطل قائمة الشاشات المفتوحة وزر الرجوع. الإصلاح: إعادة مزامنة عمق السجل عند حدث pageshow.',
      'إصلاح (متوسط): حقل الكشف عن المدفوعات المتكررة كان يُعيد الحساب في كل رسم حتى عند تغيير الفلاتر فقط — الآن يستخدم _txMutationStamp لتجنب إعادة الفحص غير الضرورية.',
      'إصلاح (متوسط): Service Worker يُفعَّل الآن بعد اكتمال التخزين المؤقت وليس أثناءه — يمنع تقديم ملفات قديمة من ذاكرة ناقصة.',
      'إصلاح (متوسط): الأرقام التي تحتوي على مسافات كفاصل آلاف ("1 000") كانت تُحلَّل خطأً كـ1 — تم دعم هذا التنسيق في normalizeDigits.',
      'إصلاح (متوسط): أسماء المحافظ المكررة كانت تُقبل إذا اختلف حجم الحروف (مثال: "حساب" و"حساب") — المقارنة الآن غير حساسة لحجم الحروف.',
      'إصلاح (متوسط): تحديث رصيد المحفظة المتتبعة يقبل قيمًا سالبة — تم إضافة حماية لرفض القيم السالبة.',
      'إصلاح (متوسط): رسالة "ترحيب المستخدم الجديد" تظهر للمستخدم العائد الذي يمسح كل معاملاته رغم وجود أرصدة — الآن تشترط أن تكون جميع الأرصدة صفرًا أيضًا.',
      'إصلاح (متوسط): إشعارات الـToast كانت تظهر خلف شريط التحديث — رُفع مستوى z-index فوقه.',
      'إصلاح (متوسط): تسجيل إشعار بالرصيد كانت تتضاعف نداءات الرسم عند التوزيع التلقائي — تم توحيدها في استدعاء واحد.',
      'إصلاح (متوسط): عداد _txMutationStamp في confirmDistribution يُحدَّث بعد التحقق من الشروط وليس قبلها.',
      'إصلاح (منخفض): وصف حصة التوزيع يعرض اسم المحفظة الإنجليزي بشكل صحيح داخل نص عربي باستخدام عزل اتجاه النص (bidi isolates).',
      'إصلاح (منخفض): رابط link لمعاملة الدخل يُضاف فقط بعد التحقق من وجود مبلغ للتوزيع.',
      'إصلاح (منخفض): نافذة التوزيع الآن لا تعرض المحفظة المصدر كهدف توزيع.',
      'إصلاح (منخفض): matchesTrackedSub تتجنب القسمة على صفر إذا كان مبلغ الاشتراك 0.',
      'إصلاح (منخفض): saveDistribution تُعيد تطبيع النسب دائمًا قبل الحفظ.',
      'تحسين: padding سفلي للصفحة معدَّل (96px → 74px) ليتطابق مع ارتفاع شريط التنقل الفعلي.',
    ],
  },
  {
    version: 'v47.27',
    date: '2026-06-24',
    title: 'تحديث تلقائي: لم يعد التطبيق يطلب موافقة لتطبيق التحديثات',
    items: [
      'تغيير: Service Worker الجديد يُفعَّل فورًا عند تثبيته (skipWaiting) دون انتظار موافقة المستخدم — الصفحة تُعاد تحميلها تلقائيًا عند تسلّمه للتحكم.',
    ],
  },
  {
    version: 'v47.26',
    date: '2026-06-24',
    title: 'إصلاح حرج: رصيد المحفظة الأساسية يصبح سالبًا بعد حذف معاملة التوزيع',
    items: [
      'إصلاح (حرج): حذف معاملة الدخل المصدر بعد التوزيع (إذا فُقد رابط link عبر مزامنة/دمج) كان يُبقي سحب التوزيع والودائع في السجل دون مصدرها، مما يجعل رصيد المحفظة الأساسية سالبًا. الإصلاح: دالة stripOrphanedDistributionLegs تكتشف هذه الأرجل المعزولة وتحذفها مع عكس أثرها على الرصيد، وتُستدعى عند الحذف والتحميل والاستيراد والدمج وإصلاح الرصيد.',
      'إصلاح: دمج البيانات (Drive وIDB) الآن يحافظ على خاصية link في المعاملة المحلية إذا كانت النسخة الواردة تفتقر إليها، مما يمنع السباق الزمني (race condition) الذي كان يفصل معاملة الدخل عن مجموعة التوزيع.',
    ],
  },
  {
    version: 'v47.25',
    date: '2026-06-24',
    title: 'إصلاح حرج: "وزّعه الآن" كان يخرج المستخدم من التطبيق',
    items: [
      'إصلاح (حرج): الضغط على "وزّعه الآن" في مودال توزيع الدخل كان يُخرج المستخدم من التطبيق تمامًا إلى صفحة خارجية — بسبب سباق (race condition) بين history.back() من إغلاق السحب وhistory.pushState() من فتح المودال، مما كان يُربك عداد التاريخ ويجعل history.back() التالي يتجاوز حدود التطبيق. الإصلاح: استبدال المعادلة back()+push() بعملية history.replaceState() ذرية تستبدل entry السحب مباشرةً بـ entry المودال.',
    ],
  },
  {
    version: 'v47.24',
    date: '2026-06-24',
    title: 'إصلاح 4 أخطاء: المراجعة اليومية، شارة المحفظة، حقل الرصيد، المعاملات المتكررة',
    items: [
      'إصلاح (متوسط): المراجعة اليومية كانت تعرض "صرفت 0.00 على 0 معاملة" عند تسجيل دخل فقط دون مصروفات — صارت الآن تعرض "لم تُسجَّل مصروفات · دخل X" بشكل صحيح.',
      'إصلاح (خفيف-متوسط): شارة نسبة محفظة التتبع كانت تعرض نسبة سالبة عند وجود رصيد سالب — صارت الآن تُعرض صفراً كحد أدنى.',
      'إصلاح (خفيف): حقل "الرصيد الجديد" في تفاصيل محفظة التتبع كان يعرض أرقاماً غير مُنسَّقة (كـ 1.1 بدل 1.10) — صار يعرض دائماً بمنزلتين عشريتين.',
      'إصلاح (خفيف): كشف المعاملات المتكررة كان يستخدم وصف أول معاملة بدل الأحدث — صار يستخدم الوصف الأحدث زمنياً.',
    ],
  },
  {
    version: 'v47.23',
    date: '2026-06-22',
    title: 'تحسين إمكانية الوصول: إعلان الأخطاء لقارئ الشاشة',
    items: [
      'إصلاح: تنبيهات الأخطاء والتحذيرات الحرجة (فشل الحفظ/الاستيراد) صارت تُعلَن لقارئ الشاشة فوراً (assertive) بدل أن تنتظر، بينما تبقى رسائل التأكيد العادية مهذّبة (polite) حتى لا تقاطع المستخدم.',
    ],
  },
  {
    version: 'v47.22',
    date: '2026-06-22',
    title: 'جولة التعريف: شريحة تخصيص + إصلاح ترجمة',
    items: [
      'إضافة شريحة جديدة لجولة التعريف الترحيبية تعرّف بميزات التخصيص التي لم تكن مذكورة: تبديل اللغة (عربي/إنجليزي)، المظهر (فاتح/داكن/أسود/تلقائي)، لون التطبيق، والمراجعة اليومية.',
      'إصلاح جذري: نقاط القوائم في جولة التعريف (وتلميح تعارض مزامنة Drive) كانت تبقى بالعربية حتى عند اختيار الإنجليزية — صارت الآن تُترجم بشكل صحيح في كلتا اللغتين.',
    ],
  },
  {
    version: 'v47.21',
    date: '2026-06-22',
    title: 'تحسين داخلي: فحص الأنواع (type-checking)',
    items: [
      'إضافة فحص أنواع آلي (TypeScript بوضع الفحص فقط، بدون أي build) يكتشف الأخطاء البرمجية وقت الكتابة — مع توثيق أنواع الدوال المالية الحرجة. كما تم تشديد بضعة مواضع كانت تعتمد على التحويل الضمني للأنواع لتصير صريحة (سلوك مطابق تماماً، أمتن مستقبلاً).',
    ],
  },
  {
    version: 'v47.20',
    date: '2026-06-22',
    title: 'تحسين داخلي: تنظيم كود الرسوم البيانية',
    items: [
      'فصل كود الرسوم البيانية (المخطط الدائري للفئات ومخطط الرصيد الخطي) إلى ملف مستقل (app.charts.js) لتقليص حجم الملف الرئيسي للواجهة وتسهيل صيانته — بدون أي تغيير في السلوك.',
    ],
  },
  {
    version: 'v47.19',
    date: '2026-06-22',
    title: 'تحسينات داخلية: اختبارات آلية وتنظيم الكود',
    items: [
      'إضافة مجموعة اختبارات آلية (وحدة + تكامل) تُشغَّل بأمر واحد للتحقق من صحة الحسابات المالية وتجربة العربي/الإنجليزي تلقائياً قبل أي تحديث — بدون أي تغيير على طريقة عمل التطبيق نفسه.',
      'تقسيم ميزة المزامنة مع Google Drive إلى ملف مستقل (app.drive.js) لتقليص حجم الملف الرئيسي وتسهيل صيانته — بدون أي تغيير في السلوك.',
    ],
  },
  {
    version: 'v47.18',
    date: '2026-06-22',
    title: 'تدقيق جديد: انعكاس التخطيط بين العربي والإنجليزي',
    items: [
      'إصلاح: في الوضع الإنجليزي (LTR)، كان مبلغ البطاقة الرئيسية ("0.00") وأرصدة المحافظ تبقى محاذاةً لليمين كأنها منفصلة عن باقي الصفحة المحاذاة لليسار — صارت الآن تُحاذى لليسار بشكل متناسق.',
      'إصلاح: أشرطة تقدّم الميزانية في كروت المحافظ كانت تمتلئ من اليمين لليسار حتى في الوضع الإنجليزي — صارت تمتلئ من اليسار لليمين كما هو المتعارف عليه في الإنجليزية.',
      'إصلاح: خط الفاصل المتلاشي بعد عناوين الأقسام (التقارير/التحليلات…) كان اتجاه تلاشيه معكوساً في الوضع الإنجليزي — صار يتلاشى بالاتجاه الصحيح في كلتا اللغتين.',
      'إصلاح: بطاقة الاشتراك التي يتجاوز يوم تحصيلها عدد أيام الشهر الحالي (مثل اليوم 31 في شهر من 30 يوماً) صارت تُوضّح اليوم الفعلي لهذا الشهر بدلاً من إظهار يوم لا يقع فيه.',
    ],
  },
  {
    version: 'v47.17',
    date: '2026-06-22',
    title: 'تدقيق شامل جديد: تجربة عربي/إنجليزي وتطابق الهوية',
    items: [
      'ترجمة عشرات النصوص المتبقية التي كانت بالعربي فقط حتى مع اختيار الإنجليزية: نافذة "المراجعة اليومية" بالكامل، نص التقرير الشهري القابل للمشاركة، مؤشر وتعارض مزامنة Drive، أسماء ألوان التطبيق (الإعدادات ← المظهر)، بطاقات الاشتراكات والمعاملات المتكررة، وأزرار "عرض المزيد".',
      'إصلاح: تلميح زر تبديل المظهر (الوضع الفاتح/الداكن) كان يظهر بالعربي دائماً بغض النظر عن اللغة المختارة.',
      'إصلاح: تنبيه "تعذّر حفظ البيانات" الحرج (عند امتلاء التخزين) لم يكن مُترجماً.',
      'إصلاح جوهري: التطبيق المثبَّت (PWA) كان يحمل اسماً واتجاهاً عربياً (RTL) ثابتاً في بياناته الوصفية بغض النظر عن اللغة المختارة — صار الآن يتبع اللغة الحالية فوراً عند تبديلها، بما يشمل اسم الأيقونة على الشاشة الرئيسية.',
      'إصلاح: اسم ملف تقرير الشهر المُنزَّل كان بالعربي دائماً حتى عند مشاركة تقرير بالإنجليزية.',
      'توحيد شكل علامة النسبة بالكروت المدمجة (الوضع البديل) مع بقية التطبيق.',
    ],
  },
  {
    version: 'v47.16',
    date: '2026-06-21',
    title: 'ترجمة كل رسائل التنبيهات وصناديق التأكيد',
    items: [
      'استكمال الترجمة إلى الإنجليزية لكل رسائل التنبيه السريعة (toast) في التطبيق — أكثر من 120 رسالة عبر إضافة/تعديل المعاملات، التحويل، المحافظ، الاشتراكات، التعرّف الصوتي، Google Drive، الاستيراد/التصدير، وإصلاح الأرصدة.',
      'ترجمة كل صناديق تأكيد الحذف/التصفير (confirm) ونوافذ كتابة كلمة التأكيد (prompt) — بما فيها كلمتا التأكيد المكتوبتان يدوياً ("تصفير"/"حذف") واللتان أصبحتا "RESET"/"DELETE" عند اختيار الإنجليزية.',
      'ترجمة نص أزرار الحفظ أثناء التنفيذ ("جارٍ الحفظ..."، "جارٍ التنفيذ...") في نماذج المعاملة والتحويل.',
    ],
  },
  {
    version: 'v47.15',
    date: '2026-06-21',
    title: 'إصلاح تزامن الترجمة عند تبديل اللغة',
    items: [
      'إصلاح: لوحات الإعدادات المفتوحة (الترتيب، تعريف المحافظ، توزيع الدخل، حالة Google Drive، شبكة الفئات) كانت تبقى بلغتها القديمة عند تبديل اللغة من داخل الإعدادات، وصارت الآن تتحدّث فوراً.',
      'إصلاح: تبويبات محرر الترتيب وعناوين الأقسام داخله كانت بالعربي دائماً حتى مع اختيار الإنجليزية — تُرجمت بالكامل.',
      'إصلاح: أسماء الفئات (طعام، مواصلات، تسوق...) كانت لا تُترجم عند اختيار الإنجليزية — أُضيفت ترجمتها في كل مكان تظهر فيه (شبكة الإضافة والتعديل، قوائم المعاملات).',
      'إصلاح جوهري: تنسيق التاريخ والوقت واسم اليوم/الشهر كان يستخدم التقويم العربي دائماً بغض النظر عن اللغة المختارة — صار يتبع اللغة الحالية في كل قوائم المعاملات والتقارير.',
      'ترجمة بطاقات "تحليلات هذا الشهر" (مصروف الشهر، المقارنة بالشهر الماضي، المتوقع نهاية الشهر) إلى الإنجليزية.',
      'ترجمة رسائل الحالات الفارغة، تسميات الفلاتر، وعناوين الوصول (aria-label) المتبقية في قوائم المعاملات والاشتراكات والتوزيع.',
    ],
  },
  {
    version: 'v47.14',
    date: '2026-06-21',
    title: 'إصلاح تعارض مزامنة Drive + تحسينات لمسات وإتاحة',
    items: [
      'إصلاح مهم: عند اتصال جهازين بـ Google Drive في نفس الوقت، صار التطبيق يتأكد من عدم وجود تحديث أحدث على السحابة قبل كل رفع تلقائي للبيانات ويدمجه بدل الكتابة فوقه — يمنع فقدان معاملات صامت كان ممكناً يحصل بين جهازين متصلين معاً.',
      'إصلاح: مقبض سحب نافذة الإضافة (لإغلاقها بالسحب لأسفل) كان معطّلاً في بعض الحالات.',
      'تكبير مساحة اللمس لأزرار نوع المعاملة وتبويبات قائمة المعاملات (المساحة المرئية لم تتغيّر، فقط سهولة الضغط عليها).',
      'تحسين تباين أزرار إعادة الترتيب المعطّلة لتكون أوضح للقراءة.',
      'تأمين عناصر التحكم في النص داخل التطبيق (وصف المعاملة في القوائم) ضد رموز اتجاه نص خبيثة قد تُربك قارئ الشاشة.',
      'تأمين استيراد البيانات واستلام نسخ Google Drive ضد قيم تالفة أو غير صالحة في قوائم المعاملات المحذوفة والمتجاهَلة.',
      'إضافة "احتواء التمرير الزائد" لقوائم المحافظ وتفاصيل المعاملات حتى لا ينزلق الصفحة كلها عند الوصول لنهاية قائمة داخلية.',
      'توحيد أيقونة عنوان نافذة الإعدادات مع بقية النوافذ، وضبط زر "تم" في لوحة المفاتيح ليناسب كل حقل رقمي وحقل البحث.',
    ],
  },
  {
    version: 'v47.13',
    date: '2026-06-21',
    title: 'استكمال ترجمة كل النوافذ المنبثقة',
    items: [
      'استكمال ترجمة كل النوافذ المنبثقة المتبقية إلى الإنجليزية: تعديل المعاملة، التحويل، تفاصيل المحفظة، توزيع الدخل، الترحيب بالتطبيق، المراجعة اليومية، تعارض المزامنة، الاشتراكات، تعريف المحفظة، وسجل التحديثات.',
      'استكمال ترجمة الإعدادات بالكامل: الترتيب، إدارة المحافظ والتوزيع، البيانات (تصدير/استيراد)، Google Drive، الصيانة، والحذف وإعادة الضبط.',
      'إصلاح بضع حالات كانت فيها عناصر تُحدَّث ديناميكياً (مثل رقم الإصدار ومبلغ التوزيع) تُكتب فوقها الترجمة أو العكس.',
    ],
  },
  {
    version: 'v47.12',
    date: '2026-06-21',
    title: 'لون مستقل لليل والنهار + استكمال الترجمة',
    items: [
      'جديد: صار لون التطبيق مستقلاً لكل وضع — اختر لوناً للنهاري وآخر لليلي/المطفي، والتبديل بين الأوضاع يستعيد لون كل وضع تلقائياً.',
      'أُعيدت تسمية وضعي "فاتح/داكن" إلى "نهاري/ليلي" في الإعدادات.',
      'استكمال ترجمة نافذة "إضافة معاملة" إلى الإنجليزية.',
    ],
  },
  {
    version: 'v47.11',
    date: '2026-06-21',
    title: 'لغة إنجليزية + تحكّم أدق بوضع الليل',
    items: [
      'جديد: دعم اللغة الإنجليزية — بدّلها من الإعدادات ← الترتيب ← اللغة، ويتحوّل اتجاه الواجهة تلقائياً (يمين↔يسار) مع اللغة. يُحفظ الاختيار ويُزامَن بين التبويبات ويُحفظ في النسخة الاحتياطية. (الترجمة تشمل الشاشات الرئيسية الآن وتتوسّع تباعاً لبقية النوافذ.)',
      'أُعيدت تسمية وضع "أسود" إلى "مطفي".',
      'صار التطبيق يتذكّر نمط الليل المفضّل لك (داكن أو مطفي): عند الضغط على زر تبديل المظهر، أو عند التبديل التلقائي حسب نظام جهازك، يذهب إلى النمط الذي اخترته آخر مرة بدل الداكن العادي دائماً.',
    ],
  },
  {
    version: 'v47.10',
    date: '2026-06-21',
    title: 'وضع أسود مطفي + لون بنّي',
    items: [
      'جديد: وضع "أسود" مطفي للّيل — خيار رابع بجانب فاتح/داكن/تلقائي في الإعدادات ← الترتيب ← المظهر. أسود محايد عميق مريح للعين وموفّر للطاقة على شاشات OLED.',
      'جديد: لون "بنّي" أُضيف إلى ألوان التطبيق — متاح في الوضعين الفاتح والداكن (والأسود) بدرجات مضبوطة على معايير التباين.',
      'كل ألوان التطبيق السبعة تعمل فوق الوضع الأسود الجديد، وتُحفظ وتُزامَن وتُستعاد كالمعتاد.',
    ],
  },
  {
    version: 'v47.9',
    date: '2026-06-21',
    title: 'ألوان للتطبيق + لمسات بصرية أدق',
    items: [
      'جديد: اختر لون التطبيق من الإعدادات ← الترتيب ← المظهر — ٦ ألوان متناغمة (ذهبي، ياقوتي، زمردي، بنفسجي، وردي، فيروزي) تُطبَّق في الوضعين الفاتح والداكن بدرجات مضبوطة على معايير التباين (WCAG AA).',
      'اللون المختار يتبعك: يُحفظ، يُزامَن فوراً بين تبويبات التطبيق المفتوحة، ويُحفظ ويُستعاد ضمن النسخة الاحتياطية (تصدير/استيراد).',
      'حركة مفتاح "الوضع البديل" صارت أنعم وأخف على البطارية (تتحرك عبر transform بدل إعادة حساب التخطيط في كل إطار).',
      'إغلاق النوافذ المنبثقة صار يتزامن فيه اختفاء الخلفية مع انزلاق النافذة بدل تفاوت بسيط بينهما.',
      'تحسين وضوح النص التلميحي داخل الحقول (placeholder)، وزيادة مساحة لمس بطاقات الفئات، وتوحيد انحناءات الزوايا في الأزرار العلوية.',
    ],
  },
  {
    version: 'v47.8',
    date: '2026-06-21',
    title: 'تحسينات إتاحة (Accessibility) ودقّة الأرقام',
    items: [
      'اختيار المحفظة من قوائم الإضافة/التعديل/التحويل صار يعمل بلوحة المفاتيح فقط (Enter/مسافة)، بعد أن كان يتطلب لمس/نقر الفأرة حصراً.',
      'مقارنة المصروف بالميزانية الشهرية صارت تتجاهل فروقات عشرية غير مرئية كانت قد تُسبب عدم ظهور تنبيه الميزانية بدقة عند حدود النسبة (80% أو 100%).',
      'حفظ ميزانية محفظة برقم بأكثر من خانتين عشريتين صار يُقرَّب بنفس طريقة باقي المبالغ في التطبيق بدل تخزينه بدقة زائدة غير متّسقة.',
      'زر "وزّعه الآن" في نافذة توزيع الدخل صار يُعطّل نفسه أثناء التنفيذ مثل بقية أزرار الحفظ، فلا يلتبس على المستخدم هل سُجّل ضغطه أم لا.',
      'الضغط على "تحديث الآن" أثناء فتح نافذة تحتوي بيانات غير محفوظة (تحويل، اشتراك، محفظة، إلخ) صار يسأل للتأكيد بدل إغلاقها وإعادة التحميل بصمت.',
    ],
  },
  {
    version: 'v47.7',
    date: '2026-06-21',
    title: 'تصحيح عطل خطير: فقدان معاملة عند فتح التطبيق بأكثر من تبويب',
    items: [
      'فتح التطبيق في تبويبين/نافذتين بنفس الوقت وإضافة معاملة مختلفة في كل منهما خلال أقل من ثانية كان أحياناً يمحو إحدى المعاملتين نهائياً من التخزين الدائم بصمت — تم إصلاحه بدمج التغييرات بدل الكتابة فوق بعضها.',
      'بعد دمج معاملات تبويبين، صار التطبيق يعيد حساب أرصدة المحافظ تلقائياً من سجل المعاملات الصحيح بدل ترك التبويب الآخر بأرقام غير متطابقة.',
      'تبويب "تلقائي" في إعدادات المظهر لم يعد ينقطع نصه على الشاشات الضيقة جداً (320px فأقل).',
      'أسماء/أوصاف المعاملات المُدخلة بخليط عربي/إنجليزي أو رموز اتجاه خفية صارت تُعرض دائماً بالاتجاه الصحيح في قائمة المعاملات ونافذة المراجعة اليومية.',
    ],
  },
  {
    version: 'v47.6',
    date: '2026-06-20',
    title: 'مظهر تلقائي يتناغم مع نظام جهازك',
    items: [
      'وضع تلقائي جديد للمظهر (فاتح/داكن) من الإعدادات ← الترتيب — يتبع إعداد نظام جهازك مباشرة، وهو الافتراضي لمن لم يختر مظهراً يدوياً من قبل.',
      'عند تبديل مظهر نظام جهازك (مثلاً تلقائياً عند الغروب) والتطبيق مفتوح، يتغيّر مظهر التطبيق فوراً بدون إعادة تحميل — طالما الوضع التلقائي مفعّل.',
      'اختيار مظهر يدوي (فاتح/داكن) من زر الشريط العلوي أو الإعدادات يوقف المتابعة التلقائية لنظام الجهاز حتى تُعيد تفعيلها بنفسك.',
      'النسخة الاحتياطية (تصدير/استيراد، ومزامنة Drive) صارت تحفظ اختيارك (تلقائي/فاتح/داكن) بدل تجميد لون واحد، فجهاز ثانٍ بالوضع التلقائي يتبع نظامه هو لا نظام الجهاز الذي صدّر النسخة.',
    ],
  },
  {
    version: 'v47.5',
    date: '2026-06-20',
    title: 'تدقيق من زوايا جديدة: استيراد، صوت وأمان',
    items: [
      'اسم محفظة يحتوي كوداً خبيثاً (مثلاً عبر مزامنة Drive من جهاز آخر) لم يعد يمكن أن يُنفَّذ — صار يُعرض كنص عادي بملاحظة "يبقى في المحفظة" بنافذة توزيع الدخل.',
      'استيراد نسخة احتياطية بملف تالف لم يعد يقدر يصفّر كل أرصدة المحافظ بصمت دون استرجاعها.',
      'استيراد معاملات بمبلغ ضخم وغير منطقي صار يُرفض عند الاستيراد، تماماً مثل الإدخال اليدوي.',
      'معاملات بدون معرّف صار تُرفض عند الاستيراد، ومعاملات بمعرّف مكرر صار يُحتفظ بأول نسخة صالحة منها فقط بدل تكرارها.',
      'الإدخال الصوتي صار يتجاهل نتيجة متأخرة تصل بعد إلغاء التسجيل، بدل تعبئة الحقول بكلام كان يُفترض إلغاؤه.',
    ],
  },
  {
    version: 'v47.4',
    date: '2026-06-20',
    title: 'تدقيق عميق: إتاحة ودقة العرض',
    items: [
      'فخّ تركيز (focus trap) جديد بكل نافذة منبثقة ودرج الإضافة — مفتاح Tab لم يعد يُخرج التركيز للخلفية المخفية أثناء فتح نافذة.',
      'محتوى الصفحة خلف أي نافذة منبثقة أو درج الإضافة صار مخفياً فعلياً عن قارئ الشاشة (aria-hidden) بدل أن يبقى ظاهراً وهو غير قابل للاستخدام.',
      'إضافة محفظة جديدة من الإعدادات صارت تُحدّث محرر توزيع الدخل فوراً، بدل احتمال بقائه بحالة قديمة حتى يحدث تحديث آخر للواجهة.',
      'نفس إصلاح التوزيع طُبّق على تصفير التوزيع الافتراضي ومزامنة محافظ جديدة قادمة من جهاز آخر عبر Drive.',
      'تسريب رابط أيقونة التطبيق المؤقت (blob) عند تبديل الوضع الفاتح/الداكن بسرعة صار مُتتبَّعاً ويُحرَّر بشكل صحيح.',
      'تنظيف كود CSS غير مستخدم لتأثير حذف معاملة بالسحب.',
    ],
  },
  {
    version: 'v47.3',
    date: '2026-06-20',
    title: 'تدقيق إضافي: لمس، تزامن وموثوقية',
    items: [
      'تعديل معاملة على جهاز ثم تعديلها على جهاز آخر صار يحتفظ بالتعديل الأحدث بدل تجاهله عند مزامنة Drive.',
      'سحب-للحذف صار يتجاهل لمسة إصبع ثانية بدل الخلط بين صفّين أثناء السحب.',
      'مقبض السحب أعلى النوافذ المنبثقة صار يعمل فعلياً للإغلاق بالسحب لأسفل.',
      'حفظ النسخة الاحتياطية المحلية صار مجمّعاً بدل تكراره مع كل حفظ صغير، لتقليل الكتابة الزائدة على القرص.',
      'تنبيه تحديث التطبيق صار يتحقق من وجود تعديل معاملة غير محفوظ، مو بس إضافة جديدة.',
      'مدة ظهور رسائل التنبيه الطويلة صارت أطول تلقائياً لإعطاء وقت كافٍ لقراءتها.',
    ],
  },
  {
    version: 'v47.2',
    date: '2026-06-20',
    title: 'تدقيق شامل: إتاحة ودقة وموثوقية',
    items: [
      'تباين ألوان أفضل بالوضع الفاتح لمبالغ المعاملات والأرصدة (كانت لا تحقق معيار الوضوح القياسي).',
      'حذف محفظة كانت تُبقي فلتر المعاملات عالقاً عليها فاضياً للأبد — صار يُمسح تلقائياً.',
      'مؤشر مزامنة Drive صار يعمل بلوحة المفاتيح، مو بس باللمس/الفأرة.',
      'منطقة لمس أكبر لزر مسح البحث، وحماية إضافية من تكرار العمليات بشاشة تفاصيل المحفظة.',
      'إصلاحات داخلية متفرقة لتقليل احتمالية تعارض المزامنة السحابية وتثبيت سلوك زر الرجوع عند تكدّس عدة نوافذ.',
    ],
  },
  {
    version: 'v47.1',
    date: '2026-06-20',
    title: 'مزامنة بدل التحديث لمحافظ التتبع',
    items: [
      'شارة محافظ التتبع بالواجهة صارت تعرض نسبة رصيد المحفظة من إجمالي كل محافظك بدل كلمة "تحديث".',
      'أيقونة جديدة ⚖️ بدل أيقونة الأسهم القديمة بكل مكان متعلق بمحافظ التتبع (الواجهة، الإعدادات، شاشة التفاصيل).',
      'كلمة "تحديث" تغيّرت إلى "مزامنة" بكل نصوص محافظ التتبع لوضوح أكبر.',
    ],
  },
  {
    version: 'v47',
    date: '2026-06-20',
    title: 'تصميم أنيق لمكان "ما الجديد؟"',
    items: [
      'زر "ما الجديد في التطبيق؟" بالإعدادات صار بطاقة بأيقونة ووصف بدل سطر مسطّح، بنفس أسلوب بقية أزرار الإعدادات.',
      'نقطة "جديد" تبرز بشكل أوضح فقط لمن عنده تحديث لم يطّلع عليه بعد.',
    ],
  },
  {
    version: 'v46',
    date: '2026-06-20',
    title: 'شارة موحّدة لكل المحافظ',
    items: [
      'محافظ عادية: زر ⓘ جديد بإعدادات → المحافظ يفتح تفاصيل المحفظة وميزانيتها مباشرة (مثل زر 🔄 لمحافظ التتبع).',
      'شارة محافظ التتبع بالواجهة والإعدادات صارت بنفس اللون والشكل الذهبي للمحافظ العادية — تصميم واحد متسق للكل.',
    ],
  },
  {
    version: 'v45',
    date: '2026-06-20',
    title: 'سجل "ما الجديد؟"',
    items: [
      'مكان جديد تحت زر التحديث القسري بالإعدادات يعرض آخر التحديثات والميزات المُضافة.',
      'نقطة "جديد" تظهر تلقائياً كلما توفر تحديث لم تطّلع عليه بعد.',
    ],
  },
  {
    version: 'v44',
    date: '2026-06-20',
    title: 'شارة معلومات أوضح للمحافظ',
    items: [
      'شارة ⓘ بجانب كل محفظة صارت شريحة بحدود وألوان واضحة بدل نص باهت، لتعرف إنها قابلة للضغط.',
      'محافظ التتبع: زر 🔄 تحديث يفتح مباشرة شاشة تحديث رصيدك الفعلي.',
      'إصلاح اقتطاع أسماء المحافظ الطويلة داخل الإعدادات.',
    ],
  },
  {
    version: 'v42',
    date: '2026-06-20',
    title: 'حذف المحافظ ودليل ترحيب محدث',
    items: [
      'زر 🗑 لحذف أي محفظة مباشرة من قائمة الترتيب بالإعدادات.',
      'شريحة جديدة بجولة الترحيب تشرح إضافة/تسمية/ترتيب/حذف المحافظ.',
    ],
  },
  {
    version: 'v41',
    date: '2026-06-20',
    title: 'الإعدادات بثلاث تبويبات',
    items: [
      'الإعدادات صارت مقسّمة: 🔀 الترتيب · 🏦 المحافظ · 💾 البيانات، بدل قائمة طويلة واحدة.',
    ],
  },
  {
    version: 'v40',
    date: '2026-06-20',
    title: 'إدارة المحافظ الكاملة',
    items: [
      'أضف محافظ جديدة (عادية تُحتسب بالإجمالي، أو تتبع فقط).',
      'عدّل اسم وترتيب أي محفظة من الإعدادات.',
    ],
  },
  {
    version: 'v34',
    date: '2026-06-19',
    title: 'ربط المصروف بمحفظة تتبع',
    items: [
      'اختياري الآن: اربط أي مصروف بمحفظة تتبع (مثل بطاقة بنكية) عند تسجيله.',
      'اختر سلوك الربط من تفاصيل المحفظة: ينقص الرصيد الفعلي، أو يزيد عدّاد إنفاق.',
    ],
  },
];
// Validates/cleans a candidate wallet-defs array (from localStorage, IndexedDB,
// an imported backup, or a Drive snapshot) before it's allowed to replace the
// live WALLET_DEFS. Returns a fresh array of plain {id,name,initial,track,pct}
// objects, or null if the input is unusable (caller should keep what it has).
function sanitizeWalletDefs(arr){
  if(!Array.isArray(arr) || !arr.length) return null;
  const seen = new Set();
  const out = [];
  arr.forEach(w => {
    if(!w || typeof w !== 'object') return;
    const id = typeof w.id === 'string' ? w.id.trim() : '';
    // Strip bidi-control/zero-width chars same as escHtml — a wallet name
    // bypasses escHtml's protection wherever it's rendered via textContent
    // (which doesn't need HTML-escaping but is still vulnerable to display
    // corruption from a name like "Cash‮hsac").
    // [...str].slice() counts Unicode code points, not UTF-16 code units — avoids
    // stranding a lone surrogate when the 40th position falls inside an emoji pair.
    const name = typeof w.name === 'string' ? [...stripBidiControls(w.name).trim()].slice(0,40).join('') : '';
    if(!id || !/^[a-zA-Z0-9_\-]+$/.test(id) || !name || seen.has(id)) return;
    seen.add(id);
    out.push({id, name, initial:0, track: !!w.track, pct: typeof w.pct === 'string' ? w.pct : (w.track ? 'تتبع' : '0%'), ...(w.crisisOnly ? {crisisOnly:true} : {})});
  });
  // Every screen that lets the user pick a spendable wallet (add form, transfers)
  // assumes at least one non-track wallet exists — a corrupt/edited blob with only
  // track wallets would otherwise brick those screens.
  if(!out.length || !out.some(w => !w.track)) return null;
  return out;
}
// Mutates WALLET_DEFS IN PLACE (clear + refill) so every other module's direct
// references to the same array — there are dozens across app.core/logic/ui.js —
// pick up the change without needing to be updated individually.
function applyWalletDefs(clean){
  WALLET_DEFS.length = 0;
  clean.forEach(w => WALLET_DEFS.push(w));
  // crisis_fund may be absent from wallet defs saved before v47.31 — always
  // ensure it exists, inserted before track wallets to preserve display order.
  const cfIdx = WALLET_DEFS.findIndex(w => w.id === 'crisis_fund');
  if(cfIdx === -1){
    const firstTrack = WALLET_DEFS.findIndex(w => w.track);
    const pos = firstTrack === -1 ? WALLET_DEFS.length : firstTrack;
    WALLET_DEFS.splice(pos, 0, {id:'crisis_fund', name:'Merged Reserve', initial:0, track:false, crisisOnly:true, pct:'0%'});
  } else if(!WALLET_DEFS[cfIdx].crisisOnly){
    WALLET_DEFS[cfIdx] = {...WALLET_DEFS[cfIdx], crisisOnly: true};
  }
  recomputeSelectableWallets();
}
// Custom wallets (added via Settings → إدارة المحافظ) are loaded synchronously
// here, before SELECTABLE_WALLETS/CRISIS_WALLET_IDS-dependent code below runs,
// so the very first render already reflects them. IndexedDB-side recovery (in
// case localStorage was wiped) happens later in loadState(), same pattern as
// the Drive client id / subscriptions recovery there.
// Set when localStorage's walletDefs key exists but is corrupted/unusable —
// loadState()'s IndexedDB-recovery check below only looked at "key absent",
// so a present-but-corrupt blob used to silently skip recovery too (worse
// than a missing key) and fall back to default wallets with zero warning.
let _walletDefsLoadFailed = false;
(function _loadCustomWalletDefsSync(){
  const raw = localStorage.getItem(LS_PREFIX + 'walletDefs');
  if(!raw) return;
  try{
    const clean = sanitizeWalletDefs(JSON.parse(raw));
    if(clean) applyWalletDefs(clean);
    else _walletDefsLoadFailed = true;
  }catch(e){ _walletDefsLoadFailed = true; }
})();

// Bar width baseline. Initials are all 0 (fresh app), so a static max is useless;
// derive the scale from the largest current non-track balance at render time so
// the bars stay proportional as real balances grow. Floor of 1 avoids /0.
function maxWalletVal(){
  let m = 1;
  WALLET_DEFS.forEach(w => {
    if(w.track) return;
    const v = parseFloat(state.wallets[w.id]) || 0;
    if(v > m) m = v;
  });
  return m;
}
// In crisis/alternative mode the budget wallets (wishlist, growth, joy, giving, …)
// are hidden and replaced by the single crisis_fund wallet.
// crisisOnly wallets are intentionally excluded from this list — they are NOT hidden
// in crisis mode (they become visible precisely when crisis mode is on).
function crisisWalletIds(){
  return WALLET_DEFS.filter(w => !w.track && w.id !== 'core' && !w.crisisOnly).map(w => w.id);
}

// `name` stays the canonical Arabic string (also used as the stable fallback
// when 'en' isn't applicable); `nameEn` is resolved through t() at lookup
// time by getCategory()/the[_makeCatChip] grid renderer, not baked in here.
const CATEGORIES = [
  {id:'food',          types:['expense'],          name:'طعام وشراب',   nameEn:'Food & drinks',  icon:'🍽️', color:'#e3a07a'},
  {id:'transport',     types:['expense'],          name:'مواصلات',      nameEn:'Transport',      icon:'🚗', color:'#86adcf'},
  {id:'shopping',      types:['expense'],          name:'تسوق',         nameEn:'Shopping',       icon:'🛍️', color:'#dcb674'},
  {id:'bills',         types:['expense'],          name:'فواتير',       nameEn:'Bills',          icon:'🧾', color:'#a78bd6'},
  {id:'health',        types:['expense'],          name:'صحة',          nameEn:'Health',         icon:'💊', color:'#86c39a'},
  {id:'entertainment', types:['expense'],          name:'ترفيه',        nameEn:'Entertainment',  icon:'🎮', color:'#e3918f'},
  {id:'salary',        types:['income'],           name:'راتب/دخل',     nameEn:'Salary/income',  icon:'💼', color:'#7fcf9f'},
  {id:'transfer',      types:['expense','income'], name:'تحويل',        nameEn:'Transfer',       icon:'🔁', color:'#9aa0ad'},
  {id:'other',         types:['expense','income'], name:'أخرى',         nameEn:'Other',          icon:'✨', color:'#8d94a3'},
];
const QUICK_AMOUNTS = [250, 500, 1000, 2000, 5000, 10000];
// `let` + explicit recompute (not a one-time const filter) because WALLET_DEFS
// can grow/shrink at runtime once wallets become user-editable, and crisis mode
// changes which wallets are visible so the add-form dropdown must match.
// PRIMARY selectable wallets are budget wallets only — tracking wallets
// (Uber/Cards/Cash) are NOT primary targets; they're assigned via the SEPARATE
// "track wallet" control (the add-form link, and the per-line track dropdown in
// quick-notes), consistently. This keeps one clear model everywhere: a primary
// (budget) wallet + an optional tracking wallet.
let SELECTABLE_WALLETS = WALLET_DEFS.filter(w => !w.track && !w.crisisOnly);
function recomputeSelectableWallets(){
  if(state.crisisMode){
    const crisisIds = new Set(crisisWalletIds());
    // crisis mode: show core + crisisOnly wallets (crisis_fund), hide normal budget wallets
    SELECTABLE_WALLETS = WALLET_DEFS.filter(w => !w.track && !crisisIds.has(w.id));
  } else {
    // normal mode: hide crisisOnly wallets (they only make sense in crisis context)
    SELECTABLE_WALLETS = WALLET_DEFS.filter(w => !w.track && !w.crisisOnly);
  }
  // If the currently selected wallet is no longer available, fall back to the first one
  if(SELECTABLE_WALLETS.length && !SELECTABLE_WALLETS.find(w => w.id === selectedWallet)){
    selectedWallet = SELECTABLE_WALLETS[0].id;
  }
}
function isTrackWallet(id){ const w = WALLET_DEFS.find(x => x.id === id); return !!(w && w.track); }
// The tracking wallets (for the secondary "track" control). Order matches WALLET_DEFS.
function trackWalletDefs(){ return WALLET_DEFS.filter(w => w.track); }

// Wallets that participate in automatic income distribution, with their share %
const DEFAULT_DISTRIBUTION = [
  {id:'core',        pct:55},
  {id:'wishlist',    pct:10},
  {id:'growth',      pct:10},
  {id:'investments', pct:10},
  {id:'joy',         pct:10},
  {id:'giving',      pct:5},
];
let DISTRIBUTION = DEFAULT_DISTRIBUTION.map(d=>({...d}));

let state = { wallets:{}, transactions:[], crisisMode:false };
let _txMutationStamp = 0;
// >0 while a multi-step async mutation is running (add/delete/distribute). The
// cross-tab storage listener checks this so another tab's save can't trigger a
// loadState() that resets `state` mid-mutation and corrupts balances.
let _opInFlight = 0;
let currentFilter = 'all';
let walletFilter = null;
let categoryFilter = null;
let selectedWallet = WALLET_DEFS[0].id;
// Optional secondary tracked-wallet a new transaction also updates (e.g. pay Uber
// from Core, and also move the linked tracked "Uber" wallet automatically). null =
// none. See applyTxToBalance + addTx + trackLinkMode.
let selectedTrackWallet = null;
let editingTxId = null;
let editType = 'expense';
let editWallet = WALLET_DEFS[0].id;
let _editingTransferLeg = false; // when true, type/category are locked (transfer)
let _editingDistSource = false; // when true, amount is locked (already-distributed income source)
let searchQuery = '';
let prevSpendable = null;
let selectedCategory = 'other';
let editCategory = 'other';
let addFormType = 'expense';
let detailWalletId = null; // when set, shows wallet detail view
let pendingIncomeTx = null;
let autoDistribute = false;
let budgets = {}; // walletId -> monthly budget limit (expenses)
// Per-tracked-wallet direction for the optional transaction link (above): 'debit'
// (a real balance — an expense DECREASES it, income increases it) or 'credit' (a
// spending counter — an expense INCREASES it). Persisted via uiPrefs. The resolved
// direction is also stamped onto each linked tx (trackSign) so later config changes
// never retroactively flip the effect of past entries.
let trackLinkMode = {}; // walletId -> 'debit' | 'credit'
let dismissedRecurring = new Set();
// Tombstones for delete propagation in multi-device merge sync: {txId: deletedAtMs}.
// Without these, a union merge would resurrect a transaction deleted on another
// device. Pruned to the last 90 days so the set stays bounded.
let deletedTxIds = {};
const TOMBSTONE_TTL_MS = 90 * 24 * 60 * 60 * 1000;
function pruneTombstones(){
  const cutoff = Date.now() - TOMBSTONE_TTL_MS;
  for(const id in deletedTxIds){ if(!(deletedTxIds[id] > cutoff)) delete deletedTxIds[id]; }
}

/* v9.3 */
let currentTab = 'home';
let addDrawerOpen = false;
let drawerTab = 0;
let subscriptions = []; // [{id, name, amount, billingDay, active}]
let editingSubId = null;

/* v9.9 — user-editable wallet definitions (add/rename/reorder) */
let editingWalletDefId = null; // null = "add new" mode in #walletDefModal
let _walletDefModalTrack = false; // pending track/regular choice while the modal is open

/* v9.4 — customizable layout (tab + section order) */
let _layoutEditorTab = 'tab'; // which sub-tab is active in the layout editor
const TAB_DEFS = {
  home:         {icon:'🏠', label:'الرئيسي',   panel:'tabHome'},
  transactions: {icon:'🧾', label:'المعاملات', panel:'tabTransactions'},
  analytics:    {icon:'📊', label:'تحليلات',   panel:'tabAnalytics'},
  reports:      {icon:'📋', label:'التقارير',  panel:'tabReports'}
};
const DEFAULT_TAB_ORDER = ['home','transactions','analytics','reports'];
// `label` is either an I18N_STRINGS key or an inline {ar,en} t() literal —
// resolved lazily via t() at render time (renderLayoutEditor) so it always
// reflects the current language instead of freezing at script-load time.
const SECTION_DEFS = {
  home: [
    {key:'balance',    label:{ar:'💰 إجمالي المتاح', en:'💰 Total available'}},
    {key:'crisis',     label:{ar:'🔄 الوضع البديل', en:'🔄 Alternate mode'}},
    {key:'quicknotes', label:{ar:'📝 ملاحظات سريعة', en:'📝 Quick notes'}},
    {key:'wallets',    label:{ar:'👛 المحافظ', en:'👛 Wallets'}}
  ],
  analytics: [
    {key:'stats',         label:'sec.monthStats'},
    {key:'recurring',     label:{ar:'🔔 تنبيهات متكررة', en:'🔔 Recurring alerts'}},
    {key:'export',        label:{ar:'📄 تصدير التقرير', en:'📄 Export report'}},
    {key:'subscriptions', label:{ar:'📆 الاشتراكات', en:'📆 Subscriptions'}},
    {key:'chart',         label:{ar:'🥧 التوزيع حسب الفئة', en:'🥧 Breakdown by category'}}
  ],
  reports: [
    {key:'summary', label:{ar:'🧮 ملخص الدخل/المصروف', en:'🧮 Income/expense summary'}},
    {key:'chart',   label:{ar:'📈 حركة الرصيد', en:'📈 Balance flow'}},
    {key:'list',    label:{ar:'🧾 قائمة المعاملات', en:'🧾 Transactions list'}}
  ]
};
let tabOrder = DEFAULT_TAB_ORDER.slice();
let sectionOrder = {}; // {home:[...], analytics:[...], reports:[...]}
const RECENT_TX_LIMIT_MAX = 50;
const RECENT_TX_LIMIT_DEFAULT = 25;
let recentTxLimit = RECENT_TX_LIMIT_DEFAULT; // how many tx the log shows per page (user-set, capped at 50)

/* SW update flow */
let _swRegistration = null;
let _pendingWorker = null;
let _reloadOnControllerChange = false;

/* ============================================================
   FORMAT HELPERS
============================================================ */
/**
 * Format a number as a 2-decimal display string with thousands separators.
 * @param {number} n
 * @returns {string}
 */
function fmt(n){
  if(isNaN(n) || !isFinite(n)) return '0.00';
  // collapse -0 and sub-cent negatives so they never render as "-0.00"
  if(Object.is(n, -0) || (n < 0 && n > -0.005)) n = 0;
  return Number(n).toLocaleString('en-US', {minimumFractionDigits:2, maximumFractionDigits:2});
}

// Build a grammatically-correct "<count> <noun>" Arabic phrase. Arabic number
// agreement: 1 → singular ("معاملة واحدة"), 2 → dual ("معاملتان"), 3-10 → plural
// ("3 معاملات"), 11+ → singular form again ("11 معاملة"). Naively concatenating
// `${count} ${singular}` is only ever correct for the 11+ case, so every count+noun
// spot in the UI needs this instead of raw template-literal interpolation.
// `singular` must stay a bare noun/short phrase — it's reused as-is in the 11+
// branch below ("15 معاملة"). Appending واحدة to it for the n===1 case only
// works when `singular` is noun-first ("معاملة" -> "معاملة واحدة"); phrases that
// lead with an adjective ("متبقية") or a verb ("ستبقى مخفية") need واحدة placed
// differently, or have no noun to attach to at all — pass the fully-formed
// phrase via `singularOne` for those instead of relying on the default.
/**
 * Build a grammatically-correct Arabic "<count> <noun>" phrase.
 * @param {number} count
 * @param {string} singular  bare noun, reused as-is for the 11+ form
 * @param {string} dual
 * @param {string} plural
 * @param {string} [singularOne]  fully-formed phrase for the count===1 case
 * @returns {string}
 */
function arPlural(count, singular, dual, plural, singularOne){
  const n = Math.abs(Number(count) || 0);
  if(n === 1) return singularOne || `${singular} واحدة`;
  if(n === 2) return dual;
  if(n >= 3 && n <= 10) return `${n} ${plural}`;
  return `${n} ${singular}`;
}

// Normalize Arabic-Indic (٠-٩) and Persian (۰-۹) digits + Arabic decimal/thousands
// separators to ASCII so amount fields accept numbers typed on Arabic keyboards.
function normalizeDigits(str){
  return String(str == null ? '' : str)
    .replace(/−/g, '-')  // Unicode minus → ASCII hyphen-minus
    .replace(/[٠-٩]/g, d => String(d.charCodeAt(0) - 0x0660)) // Arabic-Indic digits
    .replace(/[۰-۹]/g, d => String(d.charCodeAt(0) - 0x06F0)) // Extended (Persian) digits
    .replace(/[٫]/g, '.')   // Arabic decimal separator
    // A comma followed by exactly 1–2 digits at the end of the string is almost
    // certainly a decimal separator (European convention: "1,5" = 1.5, "9,99" = 9.99).
    // A comma before 3+ digits is a thousands separator ("1,500") — left for the
    // next replace to strip. This prevents "1,5" from silently becoming 15.
    .replace(/,(\d{1,2})$/, '.$1')
    .replace(/[٬,\s]/g, '');  // Arabic + Latin thousands separators + spaces ("1 000")
}
// Parse a user-entered money string robustly (Arabic numerals, separators).
// Rejects parseFloat quirks that silently create absurd balances: scientific/
// hex notation ("1e9", "0x10") and values beyond a sane money ceiling. Returns
// NaN on any rejection so every caller's existing isFinite/isNaN guard catches it.
const MAX_AMOUNT = 1e12; // one trillion — well above any realistic single entry
/**
 * Parse a user-entered money string, rejecting junk (scientific/hex notation,
 * out-of-range values) by returning NaN.
 * @param {string} str
 * @returns {number} the parsed value, or NaN on any rejection
 */
function parseAmount(str){
  const norm = normalizeDigits(str);
  if(/[a-zA-Z]/.test(norm)) return NaN; // block 1e9 / 0x10 / Infinity / NaN-style strings
  if((norm.match(/\./g) || []).length > 1) return NaN; // reject "1.2.3"
  const v = parseFloat(norm);
  if(!isFinite(v) || Math.abs(v) > MAX_AMOUNT) return NaN;
  return v;
}

// Shared by escHtml() and any other text that ends up displayed/rendered
// (e.g. wallet names) — strips Unicode bidi controls + zero-width chars. A
// pasted/imported string containing e.g. U+202E or U+200F can otherwise
// visually scramble the whole RTL layout of surrounding UI text
// ("Trojan Source"-style display corruption). Covers: zero-width
// (200B-200D, FEFF), LRM/RLM (200E-200F), Arabic letter mark (061C),
// embeddings/overrides (202A-202E), isolates (2066-2069). Explicit \u
// escapes are immune to source-editor stripping of literal controls.
function stripBidiControls(str){
  return String(str||'').replace(/[\u200B-\u200F\u061C\u202A-\u202E\u2066-\u2069\uFEFF]/g,'');
}
function escHtml(str){
  return stripBidiControls(str)
    .replace(/&/g,'&amp;')
    .replace(/</g,'&lt;')
    .replace(/>/g,'&gt;')
    .replace(/"/g,'&quot;')
    .replace(/'/g,'&#x27;');
}

// Short tactile pulse on meaningful actions (add/delete/toggle) — makes the app
// feel native. Silently no-ops where unsupported, and respects reduced-motion.
function haptic(pattern){
  try{
    if(typeof navigator !== 'undefined' && navigator.vibrate &&
       !(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches)){
      navigator.vibrate(pattern);
    }
  }catch(_){}
}

// Visual counterpart to the various _xBusy guard flags: those flags correctly
// block a double-tap from corrupting state, but gave no on-screen sign anything
// was happening, so a slow save looked like a no-op and invited a second tap.
function _setBtnSaving(btn, saving, savingText){
  if(!btn) return;
  if(saving){
    if(btn.dataset.origLabel === undefined) btn.dataset.origLabel = btn.textContent;
    btn.disabled = true;
    btn.textContent = savingText || '...';
  } else {
    btn.disabled = false;
    if(btn.dataset.origLabel !== undefined){ btn.textContent = btn.dataset.origLabel; delete btn.dataset.origLabel; }
  }
}

const _animFrames = {}; // track active animation frames per element id to allow cancellation
function animateNumber(el, from, to, duration){
  if(!el) return; // guard against missing/detached DOM element
  if(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches){
    el.textContent = fmt(to);
    return;
  }
  // key by id, falling back to a stable per-element key so two id-less elements
  // can't share the "" slot and cancel each other's frames
  const key = el.id || (el._animKey || (el._animKey = 'anon_' + Math.random().toString(36).slice(2)));
  const dur = (typeof duration === 'number' && duration > 0) ? duration : 450;
  if(_animFrames[key]) cancelAnimationFrame(_animFrames[key]);
  const start = performance.now();
  function frame(now){
    // stop animating a node that was removed mid-flight (avoids writing to a
    // detached element and leaking the rAF chain)
    if(el.isConnected === false){ delete _animFrames[key]; return; }
    const t = Math.min(1, (now - start) / dur);
    const eased = 1 - Math.pow(1 - t, 3); // ease-out cubic
    el.textContent = fmt(from + (to - from) * eased);
    if(t < 1) _animFrames[key] = requestAnimationFrame(frame);
    else delete _animFrames[key];
  }
  _animFrames[key] = requestAnimationFrame(frame);
}
/* ============================================================
   THEME (dark / light)
============================================================ */
// Cache resolved CSS theme colors — getComputedStyle() forces a synchronous
// layout flush, and the charts read these on every redraw. Invalidated whenever
// the theme changes (applyTheme below).
let _themeColorCache = {};
function themeColor(name, fallback){
  let v = _themeColorCache[name];
  if(v === undefined){
    v = getComputedStyle(document.body).getPropertyValue(name).trim() || fallback;
    _themeColorCache[name] = v;
  }
  return v;
}

// Smooth-scroll the tx list into view AFTER the current render settles (deferring
// to the next frame stops the scroll animation from competing with layout/canvas
// work, which caused visible stutter on filter/search changes)
function scrollToTxList(){
  switchTab('reports');
  const el = document.getElementById('txList');
  if(el) requestAnimationFrame(()=> el.scrollIntoView({behavior:'smooth', block:'start'}));
}

function applyTheme(theme){
  _themeColorCache = {}; // theme switch — drop cached colors so charts re-read them
  const isLight = theme === 'light';
  const isBlack = theme === 'black';
  document.body.classList.toggle('light', isLight);
  document.body.classList.toggle('theme-black', isBlack); // matte-black variant of dark
  const btn = document.getElementById('themeToggle');
  if(btn){
    btn.textContent = isLight ? '🌙' : '☀️';
    btn.title = isLight ? t({ar:'التبديل للوضع الداكن', en:'Switch to dark mode'}) : t({ar:'التبديل للوضع الفاتح', en:'Switch to light mode'});
  }
  const meta = document.querySelector('meta[name="theme-color"]');
  if(meta) meta.setAttribute('content', isLight ? '#f4f2ed' : (isBlack ? '#0b0b0d' : '#15171c'));
  // keep the installed PWA splash/chrome color in sync with the chosen theme
  if(typeof applyManifest === 'function') applyManifest(isLight);
  // day/night each keep their own accent — re-resolve for the bucket we just
  // switched into, and refresh the swatch selection if Settings is open.
  if(typeof applyAccent === 'function') applyAccent();
  if(typeof _updateAccentUI === 'function') _updateAccentUI(_currentAccent());
}
// Theme MODE is one of 'light' | 'dark' | 'black' | 'auto'. 'auto' isn't stored
// explicitly — its absence from localStorage IS the auto state, so a value written
// before this feature existed ('light'/'dark') keeps behaving as an explicit choice.
// 'black' is a manual-only matte-dark variant ('auto' never resolves to it — auto
// only follows the OS light/dark switch).
function _systemPrefersLight(){
  return !!(window.matchMedia && window.matchMedia('(prefers-color-scheme: light)').matches);
}
function _currentThemeMode(){
  let m = null;
  try{ m = localStorage.getItem(LS_PREFIX + 'theme'); }catch(e){}
  return (m === 'light' || m === 'dark' || m === 'black') ? m : 'auto';
}
// Which dark style to use whenever the theme resolves to "dark" — either the
// standard dark or the matte ('black'). Remembered from the user's last explicit
// dark/matte pick so 'auto' AND the header quick-toggle both honour it.
function _darkVariant(){
  let v = null;
  try{ v = localStorage.getItem(LS_PREFIX + 'darkVariant'); }catch(e){}
  return v === 'black' ? 'black' : 'dark';
}
function _resolveThemeMode(mode){
  if(mode === 'auto') return _systemPrefersLight() ? 'light' : _darkVariant();
  return mode;
}
function _updateThemeModeUI(mode){
  document.querySelectorAll('#themeModeTabs [data-theme-mode]').forEach(btn => {
    const active = btn.dataset.themeMode === mode;
    btn.classList.toggle('active', active);
    btn.setAttribute('aria-selected', active ? 'true' : 'false');
  });
}
function setThemeMode(mode){
  try{
    if(mode === 'auto') localStorage.removeItem(LS_PREFIX + 'theme');
    else localStorage.setItem(LS_PREFIX + 'theme', mode);
    // Remember the preferred dark style so 'auto' and the header toggle resolve
    // dark to the same variant the user last chose (standard dark vs matte).
    if(mode === 'dark' || mode === 'black') localStorage.setItem(LS_PREFIX + 'darkVariant', mode);
  }catch(e){}
  applyTheme(_resolveThemeMode(mode));
  _updateThemeModeUI(mode);
  // canvas charts bake theme colors at draw time — redraw so they match the new theme
  if(typeof renderChart === 'function') renderChart();
  if(typeof renderPieChart === 'function') renderPieChart();
}
function toggleTheme(){
  // quick header tap = explicit manual choice (the opposite of what's showing now),
  // matching the long-standing one-tap behavior; pick 'auto' from Settings instead.
  // Going to dark uses the user's preferred dark variant (standard or matte).
  const isLight = document.body.classList.contains('light');
  setThemeMode(isLight ? _darkVariant() : 'light');
}
function initTheme(){
  const mode = _currentThemeMode();
  applyTheme(_resolveThemeMode(mode));
  _updateThemeModeUI(mode);
  // live-follow the device's light/dark switch (e.g. sunset auto-dark-mode) while
  // in auto mode, instead of only resolving it once at page load
  if(window.matchMedia){
    const mq = window.matchMedia('(prefers-color-scheme: light)');
    const onSystemThemeChange = () => {
      if(_currentThemeMode() !== 'auto') return;
      applyTheme(_resolveThemeMode('auto'));
      if(typeof renderChart === 'function') renderChart();
      if(typeof renderPieChart === 'function') renderPieChart();
    };
    if(mq.addEventListener) mq.addEventListener('change', onSystemThemeChange);
    else if(mq.addListener) mq.addListener(onSystemThemeChange); // older Safari
  }
}

/* ============================================================
   ACCENT PALETTE (works in both light & dark)
============================================================ */
// Each palette re-skins the accent (--gold* + --accent-rgb) via a body.accent-<id>
// class defined in style.css (with a light variant). 'gold' is the default and
// has NO class — it falls back to the :root/body.light gold tokens. The c1/c2/on
// triple here is ONLY the swatch preview gradient shown in Settings; the authored,
// contrast-tuned applied colors live in CSS so they can differ per light/dark.
const ACCENTS = [
  {id:'gold',     name:'ذهبي',   nameEn:'Gold',     c1:'#dcb674', c2:'#b88c46', on:'#241d0d'},
  {id:'sapphire', name:'ياقوتي', nameEn:'Sapphire', c1:'#6fa0f0', c2:'#4178d0', on:'#08152b'},
  {id:'emerald',  name:'زمردي',  nameEn:'Emerald',  c1:'#54bd8a', c2:'#2f8a63', on:'#052016'},
  {id:'amethyst', name:'بنفسجي', nameEn:'Amethyst', c1:'#a987e6', c2:'#7d56c8', on:'#160a2a'},
  {id:'rose',     name:'وردي',   nameEn:'Rose',     c1:'#e985a4', c2:'#c25c7f', on:'#2a0f18'},
  {id:'teal',     name:'فيروزي', nameEn:'Teal',     c1:'#46c2c2', c2:'#2a9393', on:'#042020'},
  {id:'brown',    name:'بنّي',   nameEn:'Brown',    c1:'#b88a5e', c2:'#8a6038', on:'#1f1408'},
];
const _ACCENT_IDS = ACCENTS.map(a => a.id);
// The accent colour is remembered SEPARATELY for day vs night, so a user can run
// (say) blue in the daytime light theme and purple at night — switching the theme
// also restores that theme's own accent. 'day' = light theme; 'night' = the dark
// and matte themes (they share one night accent). Stored keys: 'accent' (day,
// kept for backward-compat with older single-accent backups) and 'accentDark'
// (night). 'gold' is the default and is represented by the key being absent.
function _accentBucket(){
  return document.body.classList.contains('light') ? 'day' : 'night';
}
function _accentKey(bucket){
  return (bucket === 'night') ? (LS_PREFIX + 'accentDark') : (LS_PREFIX + 'accent');
}
function _currentAccent(bucket){
  bucket = bucket || _accentBucket();
  let a = null;
  try{ a = localStorage.getItem(_accentKey(bucket)); }catch(e){}
  return _ACCENT_IDS.indexOf(a) > -1 ? a : 'gold';
}
// Resolve + apply the accent for the CURRENT theme bucket (no argument — it reads
// whichever bucket the active theme falls into). Called on load, on accent change,
// and at the end of every theme switch so day/night accents follow the theme.
function applyAccent(){
  const id = _currentAccent();
  _ACCENT_IDS.forEach(a => { if(a !== 'gold') document.body.classList.remove('accent-' + a); });
  if(id !== 'gold') document.body.classList.add('accent-' + id);
  _themeColorCache = {}; // accent changed --gold etc — drop cached colors
}
function _updateAccentUI(id){
  document.querySelectorAll('#accentSwatches [data-accent]').forEach(el => {
    const active = el.dataset.accent === id;
    el.classList.toggle('active', active);
    el.setAttribute('aria-checked', active ? 'true' : 'false');
  });
}
function renderAccentSwatches(){
  const wrap = document.getElementById('accentSwatches');
  if(!wrap) return;
  wrap.innerHTML = '';
  const cur = _currentAccent();
  ACCENTS.forEach(a => {
    const b = document.createElement('button');
    b.type = 'button';
    b.className = 'accent-swatch' + (a.id === cur ? ' active' : '');
    b.dataset.accent = a.id;
    b.setAttribute('role', 'radio');
    b.setAttribute('aria-checked', a.id === cur ? 'true' : 'false');
    const swatchName = t({ar: a.name, en: a.nameEn});
    b.setAttribute('aria-label', swatchName);
    b.title = swatchName;
    b.style.background = 'linear-gradient(150deg,' + a.c1 + ',' + a.c2 + ')';
    b.style.setProperty('--sw-on', a.on);
    b.onclick = () => setAccent(a.id);
    b.onkeydown = (e) => { if(e.key === 'Enter' || e.key === ' '){ e.preventDefault(); setAccent(a.id); } };
    wrap.appendChild(b);
  });
}
function setAccent(id){
  if(_ACCENT_IDS.indexOf(id) === -1) id = 'gold';
  const key = _accentKey(_accentBucket()); // write into the active theme's bucket
  try{
    if(id === 'gold') localStorage.removeItem(key);
    else localStorage.setItem(key, id);
  }catch(e){}
  applyAccent();
  _updateAccentUI(id);
  // canvas charts bake some theme colors at draw time — redraw so any accent-tinted
  // pixels stay in sync (cheap, and future-proofs charts that adopt --gold)
  if(typeof renderChart === 'function') renderChart();
  if(typeof renderPieChart === 'function') renderPieChart();
}
function initAccent(){
  applyAccent();
}

function todayISO(){
  const d = new Date();
  return d.getFullYear() + '-' + String(d.getMonth()+1).padStart(2,'0') + '-' + String(d.getDate()).padStart(2,'0');
}

// Timestamp for a transaction on the chosen date at the current time, INCLUDING
// milliseconds — so two entries within the same second still get distinct,
// correctly-ordered ts values (the sort's id tiebreaker covers any exact-ms tie).
const MIN_TX_TS = new Date('2010-01-01T00:00:00').getTime(); // matches the date pickers' min=
function buildTxTs(dateVal){
  const now = new Date();
  const ts = new Date(dateVal + 'T' + now.toTimeString().slice(0,8)).getTime();
  if(!isFinite(ts)) return Date.now(); // guard against an invalid date string
  // never allow a future ts (clock skew / DST edge) — it would sort above
  // everything and corrupt "this month" filters. Edit path already caps; do it here too.
  // Also floor at 2010 so a programmatically-set out-of-range date (the HTML min=
  // attribute is only an input-side hint) can't store a tiny/negative ts that would
  // pin the entry to the top of every list and skew monthly filters.
  return Math.max(MIN_TX_TS, Math.min(ts + now.getMilliseconds(), now.getTime()));
}

// Cap all date pickers at today so a transaction can't be accidentally future-dated
function capDateInputsToToday(){
  const t = todayISO();
  ['dateInput','editDate','transferDate'].forEach(id=>{
    const el = document.getElementById(id);
    if(el){ el.setAttribute('max', t); el.setAttribute('min', '2010-01-01'); }
  });
}

/* ============================================================
   STORAGE
============================================================ */
async function loadState(){
  state.wallets = {};
  WALLET_DEFS.forEach(w => state.wallets[w.id] = 0);
  state.transactions = [];
  state.crisisMode = false;

  let _lsHadBalances = false;
  try{
    const bal = localStorage.getItem(LS_PREFIX + 'balances');
    if(bal){
      const saved = JSON.parse(bal);
      // only restore known wallet ids — prevents orphaned keys from corrupted imports.
      // coerce to a finite number so a tampered/corrupt backup ("abc", null, 1e999→Infinity)
      // can't poison balances with NaN/Infinity that then propagates through all math.
      WALLET_DEFS.forEach(w => {
        if(saved[w.id] !== undefined){
          const v = parseFloat(saved[w.id]);
          if(isFinite(v)) state.wallets[w.id] = round2(v);
        }
      });
      _lsHadBalances = true;
    }
  }catch(e){}

  let _lsHadConfig = false;
  try{
    const cfg = localStorage.getItem(LS_PREFIX + 'config');
    if(cfg){
      const c = JSON.parse(cfg);
      state.crisisMode = !!c.crisisMode;
      autoDistribute = !!c.autoDistribute;
      if(c.budgets && typeof c.budgets === 'object'){
        budgets = {};
        WALLET_DEFS.forEach(w => {
          const v = parseFloat(c.budgets[w.id]);
          if(isFinite(v) && v > 0) budgets[w.id] = v;
        });
      }
      if(c.distribution && Array.isArray(c.distribution) && c.distribution.length){
        // sanitizeDistribution also clamps/validates each pct (a raw filter would
        // let a NaN/negative/string pct from a tampered config into the money math)
        DISTRIBUTION = sanitizeDistribution(c.distribution);
      }
      dismissedRecurring = new Set(Array.isArray(c.dismissedRecurring) ? c.dismissedRecurring : []);
      if(c.deletedTxIds && typeof c.deletedTxIds === 'object') deletedTxIds = c.deletedTxIds;
      _lsHadConfig = true;
    }
  }catch(e){}

  const _validTx = arr => (Array.isArray(arr) ? arr : []).filter(t =>
    t && t.id && (t.type === 'income' || t.type === 'expense') &&
    typeof t.ts === 'number' && isFinite(t.ts) && t.ts > 0 &&
    typeof t.amount === 'number' && isFinite(t.amount) && t.amount > 0 && t.amount <= MAX_AMOUNT &&
    WALLET_DEFS.find(w => w.id === t.wallet));

  // ── Transactions: IndexedDB is the PRIMARY store (scales far past localStorage's
  //    ~5MB cap). localStorage may still hold a legacy copy from older versions or
  //    an IDB-unavailable fallback — used only when newer than the IDB snapshot. ──
  // Wrapped (unlike a plain read) because some locked-down browsers (e.g. old Safari
  // private mode) throw on EVERY localStorage call, not just setItem — an uncaught
  // throw here would reject loadState() and leave the splash screen stuck for 6s
  // until the fatal-error watchdog kicks in, instead of degrading to IDB-only state.
  let _lsLastEdit = 0, _lsDataEdit = 0;
  try{ _lsLastEdit = parseInt(localStorage.getItem(LS_PREFIX + 'lastEdit') || '0', 10) || 0; }catch(e){}
  // Prefer dataEdit (bumped only by real DATA changes) so a pref-only save that
  // bumped lastEdit can't make a stale localStorage tx blob win over fresher IDB.
  // Fall back to lastEdit only when dataEdit is absent (legacy migration path).
  try{ _lsDataEdit = parseInt(localStorage.getItem(LS_PREFIX + 'dataEdit') || '0', 10) || 0; }catch(e){}
  const _idb = await idbRestore(); // also opens the DB, setting _idbAvailable
  // Recover custom wallet definitions from IndexedDB if localStorage's copy was
  // wiped OR corrupted (_walletDefsLoadFailed) — same wipe-recovery pattern as
  // driveClientId/subscriptions below. Must happen before _validTx/balance-restore
  // loops run (just below) so a custom wallet's transactions and balance aren't
  // silently dropped as "unknown wallet".
  if(_idb && Array.isArray(_idb.walletDefs) && (_walletDefsLoadFailed || !localStorage.getItem(LS_PREFIX + 'walletDefs'))){
    const _cleanWD = sanitizeWalletDefs(_idb.walletDefs);
    if(_cleanWD){
      applyWalletDefs(_cleanWD);
      WALLET_DEFS.forEach(w => { if(state.wallets[w.id] === undefined) state.wallets[w.id] = 0; });
      if(_idb.wallets){
        WALLET_DEFS.forEach(w => {
          if(_idb.wallets[w.id] !== undefined){
            const v = parseFloat(_idb.wallets[w.id]);
            if(isFinite(v)) state.wallets[w.id] = round2(v);
          }
        });
      }
      try{ localStorage.setItem(LS_PREFIX + 'walletDefs', JSON.stringify(WALLET_DEFS)); }catch(e){}
    }
  } else if(_walletDefsLoadFailed){
    // Corrupted locally and no usable IDB copy to recover from — the app is
    // about to fall back to default wallets, so say so instead of silently
    // dropping the user's custom wallet setup with zero indication why.
    toast(t({ar:'⚠ تعذّرت قراءة بيانات المحافظ محليًا — تم الرجوع للمحافظ الافتراضية', en:'⚠ Could not read wallet data locally — fell back to default wallets'}), true);
  }
  const _idbTime = (_idb && typeof _idb.savedAt === 'number' && isFinite(_idb.savedAt)) ? _idb.savedAt : 0;
  let _lsTx = null;
  try{ const raw = localStorage.getItem(LS_PREFIX + 'transactions'); if(raw) _lsTx = JSON.parse(raw); }catch(e){}
  const _idbHasTx = _idb && Array.isArray(_idb.transactions);
  const _lsTxNewer = Array.isArray(_lsTx) && (_lsDataEdit || _lsLastEdit) > _idbTime;

  if(_idbHasTx && !_lsTxNewer){
    // IndexedDB snapshot is the source of truth
    state.transactions = _validTx(_idb.transactions);
    // If localStorage was wiped (cleared storage), recover the small data too
    if(!_lsHadBalances && _idb.wallets){
      WALLET_DEFS.forEach(w => {
        if(_idb.wallets[w.id] !== undefined){
          const v = parseFloat(_idb.wallets[w.id]);
          if(isFinite(v)) state.wallets[w.id] = round2(v);
        }
      });
    }
    if(!_lsHadConfig){
      if(typeof _idb.crisisMode === 'boolean') state.crisisMode = _idb.crisisMode;
      if(typeof _idb.autoDistribute === 'boolean') autoDistribute = _idb.autoDistribute;
      if(_idb.budgets && typeof _idb.budgets === 'object') budgets = sanitizeBudgets(_idb.budgets);
      if(Array.isArray(_idb.dismissedRecurring)) dismissedRecurring = new Set(_idb.dismissedRecurring);
      if(_idb.distribution && Array.isArray(_idb.distribution)) DISTRIBUTION = sanitizeDistribution(_idb.distribution);
    }
    if(_idb.deletedTxIds && typeof _idb.deletedTxIds === 'object' && !Array.isArray(_idb.deletedTxIds)){
      for(const id in _idb.deletedTxIds){
        const t = _idb.deletedTxIds[id];
        if(typeof t === 'number' && (!deletedTxIds[id] || t > deletedTxIds[id])) deletedTxIds[id] = t;
      }
    }
    if(Array.isArray(_idb.subscriptions)){
      try{ localStorage.setItem(LS_PREFIX + 'subs', JSON.stringify(_idb.subscriptions)); }catch(e){}
    }
    // Same recovery for the Drive client id: without this, a wiped localStorage
    // makes driveClientId read back empty, initDrive() silently no-ops (its whole
    // body is gated on `if(driveClientId)`), and Drive sync turns itself off with
    // zero indication — the user just stops syncing and has no idea why. Restoring
    // it (but never the OAuth token, which has its own cookie fallback) lets
    // initDrive() detect "Drive was connected" and surface a normal reconnect prompt.
    if(_idb.driveClientId && !localStorage.getItem(LS_PREFIX + 'driveClientId')){
      try{ localStorage.setItem(LS_PREFIX + 'driveClientId', _idb.driveClientId); }catch(e){}
    }
    if(!_lsHadBalances && state.transactions.length) toast(t({ar:'✓ تمت استعادة البيانات من النسخ الاحتياطي', en:'✓ Data restored from backup'}));
  } else if(Array.isArray(_lsTx)){
    // Legacy localStorage copy (older version) or IDB unavailable — adopt it; the
    // idbBackup below migrates it into IndexedDB.
    state.transactions = _validTx(_lsTx);
  }

  // Drop any tx that's tombstoned (deleted on another device that synced its
  // delete) and prune expired tombstones so the set stays bounded.
  pruneTombstones();
  if(Object.keys(deletedTxIds).length){
    state.transactions = state.transactions.filter(t => !deletedTxIds[t.id]);
  }

  // A restored snapshot may contain a half-surviving linked group (one leg of a
  // transfer/distribution dropped by the validity filter). Clear the dangling
  // link so a later delete doesn't try to cascade to a partner that isn't there.
  stripOrphanLinks(state.transactions);
  if(typeof stripOrphanedDistributionLegs === 'function'){
    const _now = Date.now();
    stripOrphanedDistributionLegs(state.transactions).forEach(t => { deletedTxIds[t.id] = _now; });
  }
  _allTxSortedCache = null; // freshly replaced array — drop the sorted cache

  // If the DB couldn't be opened (blocked by another tab / corrupt) and we ended up
  // with NO transactions despite having used the app before, the data is intact in
  // IDB but unreachable. Warn the user and DO NOT run idbBackup — writing our empty
  // in-memory state could clobber the real snapshot if the DB becomes writable.
  const _hadPriorData = _lsLastEdit > 0 || _lsDataEdit > 0 || _lsHadBalances;
  const _idbLockedOut = _idbOpenFailed && state.transactions.length === 0 && _hadPriorData;
  if(_idbLockedOut){
    try{ toast(t({ar:'⚠ تعذّر فتح قاعدة البيانات — أغلق نسخ التطبيق الأخرى المفتوحة ثم أعد التحميل', en:'⚠ Could not open the database — close other open copies of the app, then reload'}), true); }catch(e){}
  }

  // Persist the consistent state into IndexedDB, then (only once confirmed) drop the
  // big legacy localStorage transactions key to free the ~5MB quota for small data.
  const _backupStamp = Math.max(_lsLastEdit, _idbTime) || Date.now();
  const _idbOk = _idbLockedOut ? false : await idbBackup(_backupStamp);
  if(_idbOk && _lsTx !== null){
    try{ localStorage.removeItem(LS_PREFIX + 'transactions'); }catch(e){}
  }

  // trackLinkMode (debit/credit direction for tracked wallets) is normally only
  // read once at startup via loadLayoutPrefs(), but it directly feeds balance
  // math (trackModeFor/applyTxToBalance). The cross-tab storage listener calls
  // loadState() for any data-relevant key — including this one — so without
  // re-reading it here a second tab keeps computing totals with a stale mode
  // after the first tab changes it, until a hard page reload.
  try{ trackLinkMode = sanitizeTrackLinkMode(JSON.parse(localStorage.getItem(LS_PREFIX + 'trackLinkMode') || 'null')); }
  catch(e){}

  _txMutationStamp++; // fresh data set loaded — invalidate any derived caches
  const _di = document.getElementById('dateInput');
  if(_di) _di.value = todayISO();
  capDateInputsToToday();
  loadSubs(_idb);
  // Rebuild the wallet dropdown to match the restored crisis-mode state — if the
  // app last closed in crisis mode, SELECTABLE_WALLETS must exclude the hidden wallets.
  recomputeSelectableWallets();
  render(true);
}

// Multi-device union merge: combine local + cloud transactions/subscriptions by id
// (so a transaction added on either device is never lost), honor tombstones from
// BOTH sides (so a deletion on either device propagates instead of resurrecting),
// then recompute balances from the merged ledger (0 + Σ — the app's model). Config
// (crisis/budgets/distribution/prefs) is taken from whichever side edited last.
// Returns {added, removed} counts for an informative toast.
function mergeCloudData(cloud, cloudNewer){
  // 0) wallet defs — additive id union FIRST (unconditionally, regardless of
  //    cloudNewer) so a wallet added on the OTHER device is already known
  //    locally before validTx below runs; otherwise its transactions would be
  //    silently rejected as "unknown wallet" and lost on this device.
  const cloudWD = Array.isArray(cloud.walletDefs) ? sanitizeWalletDefs(cloud.walletDefs) : null;
  if(cloudWD){
    const localIds = new Set(WALLET_DEFS.map(w => w.id));
    const onlyOnCloud = cloudWD.filter(w => !localIds.has(w.id));
    if(onlyOnCloud.length){
      const merged = WALLET_DEFS.concat(onlyOnCloud);
      applyWalletDefs(merged);
      onlyOnCloud.forEach(w => { if(state.wallets[w.id] === undefined) state.wallets[w.id] = 0; });
      // Reassign (not push) — computeRenderSig() caches the distribution signature by
      // object-reference equality and only re-stringifies when the reference changes;
      // an in-place push() here would silently keep the stale cached signature, so a
      // wallet synced in from another device could fail to show its new 0% share
      // until something else happens to force a full render.
      if(!cloudNewer){
        const newEntries = onlyOnCloud.filter(w => !DISTRIBUTION.find(d => d.id === w.id)).map(w => ({id: w.id, pct: 0}));
        if(newEntries.length) DISTRIBUTION = DISTRIBUTION.concat(newEntries);
      }
    }
    // Renames/reordering: only adopted from the side that edited most recently —
    // names/order are config-like, not additive data, same rule as step 5 below.
    if(cloudNewer){
      const cloudById = new Map(cloudWD.map(w => [w.id, w]));
      const renamed = WALLET_DEFS.map(w => {
        const cw = cloudById.get(w.id);
        return cw ? {...w, name: cw.name} : w;
      });
      // order: cloud ids first (in cloud order), then any local-only ids appended
      // at the end so a wallet added locally isn't dropped just because the cloud
      // snapshot predates it.
      const byId = new Map(renamed.map(w => [w.id, w]));
      const ordered = [];
      cloudWD.forEach(cw => { const w = byId.get(cw.id); if(w){ ordered.push(w); byId.delete(cw.id); } });
      byId.forEach(w => ordered.push(w));
      applyWalletDefs(ordered);
    }
  }

  const validTx = t => t && t.id && (t.type === 'income' || t.type === 'expense') &&
    typeof t.ts === 'number' && isFinite(t.ts) && t.ts > 0 &&
    typeof t.amount === 'number' && isFinite(t.amount) && t.amount > 0 && t.amount <= MAX_AMOUNT &&
    WALLET_DEFS.find(w => w.id === t.wallet);

  // 1) union tombstones from both sides
  if(cloud.deletedTxIds && typeof cloud.deletedTxIds === 'object'){
    for(const id in cloud.deletedTxIds){
      const t = cloud.deletedTxIds[id];
      if(typeof t === 'number' && (!deletedTxIds[id] || t > deletedTxIds[id])) deletedTxIds[id] = t;
    }
  }
  pruneTombstones();

  // 2) union transactions by id (local first, then cloud fills in the rest),
  //    skipping anything tombstoned on either side so deletes win over a stale copy.
  //    When an id exists on both sides, the copy with the newer editedAt wins (an
  //    edit made on one device after the last sync should overwrite the stale
  //    other-device copy); if either side lacks editedAt (data synced before this
  //    field existed) local keeps winning, same as before.
  const localCount = state.transactions.length;
  const byId = new Map();
  state.transactions.forEach(t => { if(validTx(t) && !deletedTxIds[t.id]) byId.set(t.id, t); });
  let removed = state.transactions.filter(t => deletedTxIds[t.id]).length; // local rows a remote delete removes
  let added = 0;
  (Array.isArray(cloud.transactions) ? cloud.transactions : []).forEach(t => {
    if(!validTx(t) || deletedTxIds[t.id]) return;
    const local = byId.get(t.id);
    if(!local){ byId.set(t.id, t); added++; return; }
    if(typeof t.editedAt === 'number' && typeof local.editedAt === 'number' && t.editedAt > local.editedAt){
      // Preserve the local link if the incoming version lost it (e.g. older snapshot
      // from before distribution ran).  Without this guard a Drive sync arriving
      // seconds after runDistribution would strip the link, turning the income source
      // into a standalone tx whose delete later orphans the withdrawal+deposits.
      byId.set(t.id, local.link && !t.link ? {...t, link: local.link} : t);
    }
  });
  state.transactions = [...byId.values()];
  stripOrphanLinks(state.transactions);
  if(typeof stripOrphanedDistributionLegs === 'function'){
    const _now = Date.now();
    stripOrphanedDistributionLegs(state.transactions).forEach(t => { deletedTxIds[t.id] = _now; });
  }
  _allTxSortedCache = null;

  // 4) merge subscriptions by id (union; cloud wins on a true id clash)
  const subById = new Map();
  subscriptions.forEach(s => subById.set(s.id, s));
  (Array.isArray(cloud.subscriptions) ? cloud.subscriptions : []).forEach(s => {
    if(s && s.id && s.name && isFinite(s.amount) && s.amount > 0) subById.set(s.id, _normalizeSub(s));
  });
  subscriptions = [...subById.values()];

  // 5) config from the side that edited most recently
  if(cloudNewer){
    if(typeof cloud.crisisMode === 'boolean') state.crisisMode = cloud.crisisMode;
    if(typeof cloud.autoDistribute === 'boolean') autoDistribute = cloud.autoDistribute;
    if(cloud.budgets && typeof cloud.budgets === 'object') budgets = sanitizeBudgets(cloud.budgets);
    if(cloud.distribution && Array.isArray(cloud.distribution)) DISTRIBUTION = sanitizeDistribution(cloud.distribution);
    if(cloud.uiPrefs && typeof applyUiPrefs === 'function') applyUiPrefs(cloud.uiPrefs);
  }
  // dismissedRecurring: union both sides (a dismissal on either device sticks)
  if(Array.isArray(cloud.dismissedRecurring)) cloud.dismissedRecurring.forEach(k => dismissedRecurring.add(k));

  // 6) rebuild balances from the merged ledger so they're provably consistent
  reconcileBalances();
  _txMutationStamp++;
  prevSpendable = null;
  return { added, removed, hadLocal: localCount };
}

// Recompute every (non-track) wallet balance purely from the transaction ledger
// (model: balance = 0 + Σ ledger; an expense subtracts, income/adjustment adds).
// Source of truth is the ledger, so this self-heals any drift between the stored
// balances and the transactions (from a crash mid-write, a tampered backup, or a
// rounding bug). Returns a {id: delta} diff of what changed, applies nothing
// destructive on its own beyond setting state.wallets — caller decides to persist.
// Track wallets (Uber/Cards/Cash) are skipped — their balance is intentionally
// maintained manually (see checkBalanceDrift) and can legitimately diverge from
// the ledger (real-world fees/interest never entered as a transaction). Without
// this exclusion, an automatic background Drive merge would silently blow away a
// manually-set tracked balance back to the ledger sum.
function reconcileBalances(){
  const computed = {}, diff = {};
  WALLET_DEFS.forEach(w => { if(!w.track) computed[w.id] = 0; }); // baseline 0 per the app's model
  state.transactions.forEach(tx => {
    if(computed[tx.wallet] === undefined) return; // skip unknown/track wallet ids
    const amt = parseFloat(tx.amount);
    if(!isFinite(amt)) return;
    computed[tx.wallet] = round2(computed[tx.wallet] + (tx.type === 'expense' ? -amt : amt));
  });
  WALLET_DEFS.forEach(w => {
    if(w.track) return; // leave manually-tracked balances untouched
    const before = parseFloat(state.wallets[w.id]) || 0;
    const after = computed[w.id];
    if(Math.abs(after - before) >= 0.005) diff[w.id] = round2(after - before);
    state.wallets[w.id] = after;
  });
  return diff;
}

// dataEdit marks the last change to actual user DATA (balances/transactions/subs),
// distinct from lastEdit which any save (incl. config/prefs) bumps. Drive conflict
// resolution compares dataEdit so a pref-only change (crisis toggle, layout) can't
// make a stale local copy "win" over fresher cloud transaction data.
function stampDataEdit(ts){ try{ localStorage.setItem(LS_PREFIX + 'dataEdit', String(ts)); }catch(e){} }
async function saveBalances(){
  const ts = Date.now();
  let lsOk = false;
  try{
    localStorage.setItem(LS_PREFIX + 'balances', JSON.stringify(state.wallets));
    localStorage.setItem(LS_PREFIX + 'lastEdit', String(ts));
    lsOk = true;
  }catch(e){
    toast(t({ar:'⚠ فشل الحفظ المحلي — يتم الحفظ في النسخة الاحتياطية', en:'⚠ Local save failed — saving to the backup copy'}), true);
  }
  if(lsOk) stampDataEdit(ts);
  scheduleDriveSync();
  scheduleIdbBackup(ts);
}
async function saveTx(){
  _allTxSortedCache = null;
  const ts = Date.now();
  // Transactions are stored in IndexedDB (idbBackup below) so they scale far past
  // the ~5MB localStorage cap. Here we only stamp the small timestamps. If IDB is
  // unavailable, idbBackup() mirrors the array to localStorage as a fallback.
  try{ localStorage.setItem(LS_PREFIX + 'lastEdit', String(ts)); }catch(e){}
  stampDataEdit(ts);
  scheduleDriveSync();
  scheduleIdbBackup(ts);
}
function _pruneRecurringDismissals(){
  if(dismissedRecurring.size < 40) return;
  // dismissal keys are "desc\x00walletId" (see detectRecurring, which keys on
  // normalizeSearch(desc)) — build the live set with the SAME shape, otherwise
  // NONE of the keys ever match and we wipe every dismissal at once, making
  // dismissed suggestions reappear.
  const live = new Set(
    state.transactions
      .map(tx => normalizeSearch(tx.desc) + '\x00' + tx.wallet)
      .filter(k => k.charAt(0) !== '\x00') // drop empty-description keys
  );
  for(const k of dismissedRecurring){ if(!live.has(k)) dismissedRecurring.delete(k); }
  // Hard cap so a user with 200+ unique recurring patterns never exhausts localStorage
  // quota (all keys match live transactions so the live-set pruning above removes nothing)
  while(dismissedRecurring.size > 200) dismissedRecurring.delete(dismissedRecurring.values().next().value);
}
async function saveConfig(){
  const ts = Date.now();
  _pruneRecurringDismissals();
  pruneTombstones();
  let ok = false;
  try{
    localStorage.setItem(LS_PREFIX + 'config', JSON.stringify({crisisMode: state.crisisMode, autoDistribute: autoDistribute, budgets: budgets, dismissedRecurring: Array.from(dismissedRecurring), distribution: DISTRIBUTION, deletedTxIds: deletedTxIds}));
    localStorage.setItem(LS_PREFIX + 'lastEdit', String(ts));
    ok = true;
  }catch(e){ toast(t({ar:'⚠ فشل حفظ الإعدادات محليًا', en:'⚠ Failed to save settings locally'}), true); }
  scheduleDriveSync();
  scheduleIdbBackup(ts);
  return ok;
}
// clamp a subscription's billing day into 1–31 so corrupt data can't produce
// "يوم 99" that never matches the daily-review check
function _normalizeSub(x){
  let d = parseInt(x.billingDay, 10);
  if(!isFinite(d)) d = 1;
  x.billingDay = Math.min(31, Math.max(1, d));
  return x;
}
function loadSubs(idbSnapshot){
  try{
    const s = localStorage.getItem(LS_PREFIX + 'subs');
    if(s) subscriptions = JSON.parse(s)
      .filter(x => x && x.id && x.name && isFinite(x.amount) && x.amount > 0)
      .map(_normalizeSub);
  }catch(e){
    // localStorage's 'subs' key is corrupted (e.g. a crash mid-write) — fall back
    // to the IndexedDB snapshot's copy (idbBackup mirrors subscriptions into it on
    // every save) instead of silently wiping the user's recurring subscriptions.
    if(idbSnapshot && Array.isArray(idbSnapshot.subscriptions) && idbSnapshot.subscriptions.length){
      subscriptions = idbSnapshot.subscriptions
        .filter(x => x && x.id && x.name && isFinite(x.amount) && x.amount > 0)
        .map(_normalizeSub);
      toast(t({ar:'⚠ تعذّرت قراءة الاشتراكات محليًا — تم استرجاعها من النسخة الاحتياطية', en:'⚠ Could not read subscriptions locally — recovered from the backup copy'}), true);
    } else {
      subscriptions = [];
      toast(t({ar:'⚠ تعذّرت قراءة الاشتراكات المحفوظة، تم البدء بقائمة فارغة', en:'⚠ Could not read saved subscriptions — starting with an empty list'}), true);
    }
  }
}
async function saveSubs(){
  const ts = Date.now();
  try{ localStorage.setItem(LS_PREFIX + 'subs', JSON.stringify(subscriptions)); }catch(e){ toast(t({ar:'⚠ فشل حفظ الاشتراكات محليًا', en:'⚠ Failed to save subscriptions locally'}), true); }
  stampDataEdit(ts);
  scheduleDriveSync();
  scheduleIdbBackup(ts);
}
async function saveWalletDefs(){
  const ts = Date.now();
  try{ localStorage.setItem(LS_PREFIX + 'walletDefs', JSON.stringify(WALLET_DEFS)); }catch(e){ toast(t({ar:'⚠ فشل حفظ بيانات المحافظ محليًا', en:'⚠ Failed to save wallet data locally'}), true); }
  stampDataEdit(ts);
  scheduleDriveSync();
  scheduleIdbBackup(ts);
}

/* ============================================================
   INDEXEDDB BACKUP (extra resilience alongside localStorage)
============================================================ */
let _idbInstance = null;
let _idbAvailable = false; // becomes true once the DB opens — gates IDB-primary storage
let _idbOpenPromise = null; // in-flight open() shared by concurrent early callers
function idbOpen(){
  if(_idbInstance) return Promise.resolve(_idbInstance);
  // Without this cache, two saves fired back-to-back before the FIRST-EVER open()
  // resolves (e.g. loadState's idbRestore racing an early saveBalances) would each
  // issue their own indexedDB.open() and end up with two separate connection
  // objects to the same DB — their later writes are then only ordered by whichever
  // connection's request callback the browser happens to fire first, not by call
  // order, which can let an older write silently land after a newer one.
  if(_idbOpenPromise) return _idbOpenPromise;
  _idbOpenPromise = new Promise((resolve, reject)=>{
    if(!('indexedDB' in window)){ reject('no idb'); return; }
    const req = indexedDB.open('walletTrackerDB', 1);
    req.onupgradeneeded = () => {
      req.result.createObjectStore('backup');
    };
    req.onsuccess = () => { _idbInstance = req.result; _idbAvailable = true; resolve(_idbInstance); };
    req.onerror   = () => reject(req.error);
    // Fires when another tab holds the DB at an older version and hasn't closed
    // it — without this the open request hangs indefinitely.
    req.onblocked = () => reject(new Error('idb blocked'));
  }).finally(() => { _idbOpenPromise = null; });
  return _idbOpenPromise;
}
// Writes the full snapshot to IndexedDB — the PRIMARY store for transactions
// (localStorage only holds the small balances/config/prefs). Returns true on a
// confirmed write so callers can safely migrate/free the legacy localStorage copy.
let _idbWriteInFlight = 0; // >0 while an IDB snapshot write is committing
async function idbBackup(savedAt){
  _idbWriteInFlight++;
  try{
    const db = await idbOpen();
    await new Promise((resolve, reject)=>{
      const tx = db.transaction('backup','readwrite');
      const store = tx.objectStore('backup');
      const getReq = store.get('snapshot');
      getReq.onsuccess = () => {
        const existing = getReq.result;
        // Two tabs of the same app can each debounce-write within the same ~400ms
        // window; without this, whichever write commits last would blindly overwrite
        // the other tab's already-persisted transactions (each tab only knows its
        // OWN in-memory copy). Union by id against whatever's currently in IDB —
        // same tombstone + newer-editedAt-wins rule as mergeCloudData — so a
        // transaction added in the other tab survives instead of being silently
        // erased. This get+put runs inside one IDB transaction, which browsers
        // serialize against the other tab's own transaction on this store, so the
        // merge sees a consistent snapshot rather than racing the read itself.
        let mergedTx = state.transactions;
        let mergedTombstones = deletedTxIds;
        if(existing && existing.deletedTxIds && typeof existing.deletedTxIds === 'object'){
          mergedTombstones = Object.assign({}, deletedTxIds);
          for(const id in existing.deletedTxIds){
            const t = existing.deletedTxIds[id];
            if(typeof t === 'number' && (!mergedTombstones[id] || t > mergedTombstones[id])) mergedTombstones[id] = t;
          }
          deletedTxIds = mergedTombstones;
        }
        if(existing && Array.isArray(existing.transactions)){
          const byId = new Map();
          state.transactions.forEach(t => { if(t && t.id && !mergedTombstones[t.id]) byId.set(t.id, t); });
          existing.transactions.forEach(t => {
            if(!t || !t.id || mergedTombstones[t.id]) return;
            const local = byId.get(t.id);
            if(!local){ byId.set(t.id, t); return; }
            if(typeof t.editedAt === 'number' && typeof local.editedAt === 'number' && t.editedAt > local.editedAt){
              // Preserve link from local if the IDB snapshot lost it
              byId.set(t.id, local.link && !t.link ? {...t, link: local.link} : t);
            }
          });
          mergedTx = [...byId.values()];
        }
        store.put({
          wallets: state.wallets,
          walletDefs: WALLET_DEFS,
          transactions: mergedTx,
          crisisMode: state.crisisMode,
          autoDistribute, budgets,
          distribution: DISTRIBUTION,
          dismissedRecurring: Array.from(dismissedRecurring),
          deletedTxIds: mergedTombstones,
          subscriptions: subscriptions,
          // mirrored so a wiped localStorage can still recover "Drive was connected"
          // (see loadState) instead of Drive sync silently going dark with no UI cue
          driveClientId: driveClientId,
          savedAt: (typeof savedAt === 'number' && isFinite(savedAt)) ? savedAt : Date.now()
        }, 'snapshot');
      };
      getReq.onerror = () => reject(getReq.error);
      tx.oncomplete = () => resolve();
      tx.onerror = () => reject(tx.error);
      tx.onabort = () => reject(tx.error || new Error('idb abort'));
    });
    _idbAvailable = true;
    return true;
  }catch(e){
    // IndexedDB unavailable/failed (e.g. private mode) — fall back to a localStorage
    // mirror so transactions still persist (bounded by the ~5MB quota in that case).
    _idbAvailable = false;
    try{
      localStorage.setItem(LS_PREFIX + 'transactions', JSON.stringify(state.transactions));
    }catch(_){
      // BOTH IndexedDB and the localStorage fallback failed — the latest change is
      // memory-only and will be lost on reload. This fires AFTER the optimistic
      // success toast (idbBackup runs unawaited in the background from
      // saveTx/saveBalances/etc, so render()+toast('✓ ...') already happened).
      // A plain toast() here would just overwrite/be overwritten by whatever
      // routine toast fires next (e.g. the distribution-flow toast in addTx) and
      // vanish in 2.2s — easy to miss for a data-loss-grade warning. Use
      // toastWithAction: longer-lived (5s), gives a real recovery step, and is
      // visually distinct from a routine success/error toast.
      // Re-arms every few minutes instead of firing once per session — a user
      // stuck in this state keeps losing every subsequent edit, so a one-shot
      // warning would go silent on them after the first toast while the drift
      // (unsaved changes piling up) continues unannounced.
      if(Date.now() - _persistFailWarnedAt > PERSIST_FAIL_REWARN_MS){
        _persistFailWarnedAt = Date.now();
        try{
          toastWithAction(t({ar:'⚠ تعذّر حفظ البيانات على هذا الجهاز — صدّرها الآن قبل إغلاق التطبيق', en:'⚠ Couldn\'t save data on this device — export it now before closing the app'}), t({ar:'تصدير الآن', en:'Export now'}), () => { try{ exportData(); }catch(e){} }, true);
        }catch(__){}
      }
    }
    return false;
  }finally{
    _idbWriteInFlight--;
  }
}
// saveBalances/saveTx/saveConfig/saveSubs/saveWalletDefs each fire idbBackup()
// independently, so a single user action that touches more than one (e.g.
// deleteTx → saveBalances + saveTx + saveConfig) used to write the entire
// transactions array to IndexedDB 2-3x in a row. Debounce into one coalesced
// write — flushed immediately on tab-hide/page-unload (see visibilitychange
// and beforeunload below) so a backgrounded/closed tab never loses the pending
// write entirely.
let _idbBackupTimer = null;
let _idbBackupPendingTs = 0;
function scheduleIdbBackup(ts){
  _idbBackupPendingTs = (typeof ts === 'number' && isFinite(ts)) ? ts : Date.now();
  clearTimeout(_idbBackupTimer);
  _idbBackupTimer = setTimeout(()=>{ _idbBackupTimer = null; idbBackup(_idbBackupPendingTs); }, 400);
}
function flushIdbBackup(){
  if(_idbBackupTimer){ clearTimeout(_idbBackupTimer); _idbBackupTimer = null; idbBackup(_idbBackupPendingTs); }
}
// Re-arming cooldown (not a one-shot flag) for the "could not persist" warning
// — see the toastWithAction call above for why a single lifetime-of-session
// warning isn't enough here.
const PERSIST_FAIL_REWARN_MS = 3 * 60 * 1000;
let _persistFailWarnedAt = 0;
let _idbOpenFailed = false; // true when the DB exists but couldn't be opened (blocked/corrupt)
async function idbRestore(){
  try{
    const db = await idbOpen();
    // A successful open clears any earlier failure latch — otherwise a
    // transient open failure (e.g. another tab briefly holding an older DB
    // version during startup) would permanently arm the "data locked out"
    // warning for the rest of the session even after IDB recovers.
    _idbOpenFailed = false;
    return new Promise((resolve)=>{
      const tx = db.transaction('backup','readonly');
      const req = tx.objectStore('backup').get('snapshot');
      req.onsuccess = () => resolve(req.result || null);
      req.onerror = () => resolve(null);
    });
  }catch(e){
    // Distinguish "IndexedDB unsupported" (expected → LS fallback) from a real open
    // failure such as onblocked (another tab holds an older version) or corruption.
    // The latter means the user's data is SAFE in IDB but unreachable right now —
    // loadState must warn rather than present an empty app and must NOT overwrite it.
    if(e !== 'no idb') _idbOpenFailed = true;
    return null;
  }
}


function toggleCrisis(){
  state.crisisMode = !state.crisisMode;
  if(walletFilter){
    const _wf = WALLET_DEFS.find(x => x.id === walletFilter);
    const _hidden = state.crisisMode
      ? crisisWalletIds().includes(walletFilter)
      : (_wf && _wf.crisisOnly);
    if(_hidden) walletFilter = null;
  }
  const _ct = document.getElementById('crisisToggle');
  if(_ct) _ct.setAttribute('aria-checked', state.crisisMode ? 'true' : 'false'); // may be hidden via layout editor
  // Rebuild the wallet dropdown so it only shows wallets visible in the current mode
  recomputeSelectableWallets();
  // crisis flips the spendable total by the reserve amount — that's not a real
  // money movement, so snap to the new value instead of count-up animating across it
  prevSpendable = null;
  saveConfig();
  render();
  haptic(15);
  toast(state.crisisMode ? t({ar:'🔄 تم تفعيل الوضع البديل', en:'🔄 Crisis mode enabled'}) : t({ar:'✓ تم إيقاف الوضع البديل', en:'✓ Crisis mode disabled'}));
}

